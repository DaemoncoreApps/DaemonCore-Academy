const references = {
  nist: { label: 'NIST SP 800-115 — Technical Guide to Information Security Testing', url: 'https://csrc.nist.gov/pubs/sp/800/115/final' },
  nmap: { label: 'Nmap Reference Guide', url: 'https://nmap.org/book/man.html' },
  curl: { label: 'curl tool manual', url: 'https://curl.se/docs/manpage.html' },
  openssl: { label: 'OpenSSL s_client documentation', url: 'https://docs.openssl.org/master/man1/openssl-s_client/' },
  wstg: { label: 'OWASP Web Security Testing Guide', url: 'https://owasp.org/www-project-web-security-testing-guide/latest/' },
  microsoft: { label: 'Microsoft Active Directory Security Assessment', url: 'https://learn.microsoft.com/en-us/services-hub/microsoft-engage-center/health/getting-started-adsecurity' },
  aws: { label: 'AWS IAM policy evaluation logic', url: 'https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_evaluation-logic.html' },
  kubernetes: { label: 'Kubernetes Security Checklist', url: 'https://kubernetes.io/docs/concepts/security/security-checklist/' },
  cisa: { label: 'CISA Known Exploited Vulnerabilities Catalog', url: 'https://www.cisa.gov/known-exploited-vulnerabilities-catalog' },
}

const step = (title, instruction, command, expected, analysis) => ({ title, instruction, command, expected, analysis })

export const lessonPracticals = {
  'rules-of-engagement': {
    level: 'FOUNDATION', environment: 'OFFLINE SCOPE WORKBENCH',
    outcome: 'Convert a signed scope into a machine-usable test boundary and stop conditions.',
    objectives: ['Separate authorization from technical reachability', 'Normalize targets, techniques, windows, and exclusions', 'Write stop conditions another operator can execute'],
    prerequisites: ['A synthetic statement of work', 'A text editor', 'No network access required'],
    steps: [
      step('Extract the five control fields', 'Read the authorization once for intent, then again for exact assets, techniques, timing, data rules, and escalation contacts.', `TARGETS: archive-target\nTECHNIQUES: service discovery, HTTP inspection\nWINDOW: 2026-08-26 14:00Z–18:00Z\nEXCLUSIONS: availability tests, credential guessing\nSTOP: instability, private data, scope ambiguity`, `A short boundary record with no implied permissions.`, 'If a technique or target is absent, it is not quietly assumed. Mark it unresolved and escalate.'),
      step('Turn prose into allowlists', 'Normalize hostnames, ports, and time to values a tool can compare exactly.', `archive-target | tcp/22,445,8088 | 14:00Z–18:00Z`, `One target row; three explicit ports; one UTC window.`, 'Machine-readable boundaries eliminate interpretation at the moment a command runs.'),
      step('Pre-write the stop message', 'Create the exact note you will send if a boundary trips so stopping does not depend on improvisation.', `STOPPED 15:42Z — target resolved outside approved range. No follow-on action taken. Evidence E-03 preserved. Awaiting scope decision.`, `A timestamped, factual escalation with the action already halted.`, 'A good stop note says what happened, what did not happen, and what decision is required.'),
    ],
    exercise: { brief: 'Review a synthetic scope containing a wildcard domain and an availability-testing exclusion.', deliverable: 'Produce an exact target/port matrix plus three stop conditions.', success: ['No wildcard is silently expanded', 'Availability testing remains excluded', 'Every stop condition names an escalation action'] },
    references: [references.nist],
  },
  'recon-hypotheses': {
    level: 'FOUNDATION', environment: 'SEALED GHOST PORT RANGE',
    outcome: 'Drive reconnaissance from questions and choose the smallest test that changes the asset model.',
    objectives: ['Write falsifiable service hypotheses', 'Select narrow probes before broad collection', 'Record confidence changes after each result'],
    prerequisites: ['Ghost Port range running', 'Authorization boundary reviewed', 'Basic TCP vocabulary'],
    steps: [
      step('Start with the inventory delta', 'Read the supplied record before probing anything.', `cat /opt/mission/inventory.txt`, `dc-archive-02  22/tcp, 445/tcp`, 'The inventory predicts two listeners. That prediction gives the observation phase a baseline.'),
      step('Ask one bounded question', 'Test only the declared and suspected ports on the synthetic target.', `nmap -sT -Pn -p 22,445,8088 archive-target`, `22/tcp open\n445/tcp open\n8088/tcp open`, 'The result answers whether the suspected coordinate responds. It does not yet identify the application.'),
      step('Resolve the highest-value uncertainty', 'Use service detection only on the anomalous port.', `nmap -sV -Pn -p 8088 archive-target`, `8088/tcp open  http  Archive Console`, 'Confidence moves from “unexpected listener” to “likely HTTP service.” HTTP evidence is the next smallest test.'),
    ],
    exercise: { brief: 'Build a three-row hypothesis ledger for ports 22, 445, and 8088.', deliverable: 'For each row: prediction, observation, confidence, and next safe test.', success: ['Observation is distinct from inference', 'Each next action answers a named uncertainty', 'No out-of-scope ports are added'] },
    references: [references.nmap, references.nist],
  },
  'asset-inventory': {
    level: 'FOUNDATION', environment: 'OFFLINE DATA WORKBENCH',
    outcome: 'Produce a deduplicated asset record with provenance and meaningful deltas.',
    objectives: ['Normalize multiple identifiers for one asset', 'Keep declared and observed state separate', 'Attach source and timestamp to every fact'],
    prerequisites: ['Synthetic inventory file', 'Synthetic DNS and scan observations'],
    steps: [
      step('Create a canonical key', 'Choose the durable identifier, then attach hostname, IP, certificate name, owner, and environment as aliases.', `asset_id: dc-archive-02\naliases: [archive-target, 10.40.7.23]\nowner: Platform Ops`, `One asset, multiple identifiers, one owner.`, 'Do not let every observation create a new row. Identity resolution comes before counting assets.'),
      step('Separate columns by provenance', 'Record declared values without overwriting them with scan results.', `declared_ports: [22,445]\nobserved_ports: [22,445,8088]\nobserved_at: 2026-08-26T15:14:00Z`, `The unexpected value remains visible as a delta.`, 'Merging the lists would erase the exact signal the assessment is meant to find.'),
      step('Classify the delta', 'Assign an owner and a disposition rather than treating every difference as a vulnerability.', `delta: 8088/tcp\nstatus: owner-review\nevidence: E-02\nquestion: approved service or shadow exposure?`, `A reviewable exception with a next decision.`, 'Inventory findings are governance signals until technical impact is validated.'),
    ],
    exercise: { brief: 'Reconcile five supplied aliases into three real systems.', deliverable: 'A normalized JSON inventory and a separate delta list.', success: ['Aliases are not double-counted', 'Every observed value has a timestamp', 'Unresolved ownership remains explicit'] },
    references: [references.nist],
  },
  'packet-evidence': {
    level: 'FOUNDATION', environment: 'OFFLINE PCAP WORKBENCH',
    outcome: 'Read a TCP conversation without inventing causes from missing packets.',
    objectives: ['Follow direction and sequence across a conversation', 'Distinguish reset, timeout, and capture absence', 'Preserve narrow evidence with acquisition context'],
    prerequisites: ['Synthetic PCAP', 'Wireshark or tshark installed'],
    steps: [
      step('Reduce the capture to the question', 'Filter one host and port before interpreting state.', `tshark -r ghost-port.pcapng -Y "ip.addr==10.40.7.23 && tcp.port==8088" -T fields -e frame.time_relative -e ip.src -e tcp.flags`, `0.000 10.40.7.50 0x0002\n0.003 10.40.7.23 0x0012\n0.004 10.40.7.50 0x0010`, 'The SYN, SYN-ACK, ACK sequence directly supports a completed TCP handshake.'),
      step('Follow the application exchange', 'Add stream and payload-length fields to see request/response direction.', `tshark -r ghost-port.pcapng -Y "tcp.stream==4" -T fields -e ip.src -e tcp.len`, `10.40.7.50 87\n10.40.7.23 214`, 'Direction and length show an exchange but not its semantic content. Decode only when authorized.'),
      step('Hash the original', 'Preserve the acquisition artifact before annotations or exports.', `sha256sum ghost-port.pcapng`, `7f3c…  ghost-port.pcapng`, 'Record interface, capture window, filter, tool version, and hash beside the file.'),
    ],
    exercise: { brief: 'Classify three synthetic flows: successful handshake, explicit reset, and no observed response.', deliverable: 'A timeline and a one-sentence conclusion for each flow.', success: ['No response is not labeled closed', 'Every conclusion cites packet evidence', 'The original capture hash is recorded'] },
    references: [references.nist],
  },
  'ports-protocols': {
    level: 'FOUNDATION', environment: 'SEALED GHOST PORT RANGE',
    outcome: 'Interpret transport states as measurement results rather than service identity.',
    objectives: ['Explain open, closed, and filtered in method context', 'Compare TCP connect results with application behavior', 'Avoid port-number assumptions'],
    prerequisites: ['Ghost Port range running', 'Nmap installed in operator container'],
    steps: [
      step('Measure transport state', 'Use a TCP connect scan so the method is explicit and reproducible without raw-packet assumptions.', `nmap -sT -Pn -p 22,445,8088 archive-target`, `22/tcp open ssh\n445/tcp open microsoft-ds\n8088/tcp open http-alt`, 'The service column may be a conventional name. The state column reflects the measurement.'),
      step('Test the coordinate directly', 'Open a TCP connection and preserve the first bytes returned.', `nc -nv archive-target 22`, `SSH-2.0-OpenSSH_9.3 DaemonCore_Training`, 'The banner supports an SSH-family protocol hypothesis; it does not prove patch state or secure configuration.'),
      step('Challenge the port convention', 'Speak HTTP to the nonstandard listener rather than assuming “http-alt.”', `curl -i http://archive-target:8088/health`, `HTTP/1.1 200 OK\nServer: Archive Console/0.8-training\n\nhealthy`, 'Protocol behavior and response structure provide stronger identification than the port number.'),
    ],
    exercise: { brief: 'Write a state table for all three ports with observation, inference, and confidence.', deliverable: 'A table that never uses “vulnerable” as a port state.', success: ['Method and vantage are named', 'Port convention is separated from protocol evidence', 'Confidence reflects corroboration'] },
    references: [references.nmap, references.curl],
  },
  'service-fingerprinting': {
    level: 'FOUNDATION', environment: 'SEALED GHOST PORT RANGE',
    outcome: 'Triangulate a service identity from independent signals and state confidence honestly.',
    objectives: ['Collect banner, protocol, header, and inventory signals', 'Detect disagreement between sources', 'Report identity with calibrated confidence'],
    prerequisites: ['Ghost Port range running', 'Completed ports and protocols lesson'],
    steps: [
      step('Collect the scanner hypothesis', 'Run version detection only on the approved anomalous listener.', `nmap -sV --version-light -Pn -p 8088 archive-target`, `8088/tcp open http Archive Console`, 'Version detection is a probe-derived hypothesis, not ground truth.'),
      step('Collect native protocol evidence', 'Request a harmless status endpoint and include headers.', `curl -sS -D - http://archive-target:8088/status`, `HTTP/1.1 200 OK\nServer: Archive Console/0.8-training\nContent-Type: application/json`, 'The HTTP syntax, Server header, and JSON schema are independent signals.'),
      step('Write confidence, not certainty', 'Combine signals and name what remains unverified.', `Observed HTTP on 8088. Header identifies Archive Console/0.8-training; status schema agrees. Identity confidence: high. Build integrity and patch state: not tested.`, `A bounded conclusion that survives review.`, 'The untested properties matter as much as the supported identity claim.'),
    ],
    exercise: { brief: 'Reconcile a misleading port label with a reliable protocol response.', deliverable: 'A four-signal fingerprint table and a confidence statement.', success: ['At least two independent signals agree', 'Conflicting evidence is preserved', 'Untested properties are named'] },
    references: [references.nmap, references.curl],
  },
  'evidence-quality': {
    level: 'FOUNDATION', environment: 'OFFLINE EVIDENCE WORKBENCH',
    outcome: 'Create an evidence item another operator can reproduce and audit.',
    objectives: ['Capture command, context, raw result, and interpretation separately', 'Hash artifacts and minimize sensitive data', 'Build a reproducible validation recipe'],
    prerequisites: ['Synthetic command output', 'A clean evidence directory'],
    steps: [
      step('Capture provenance first', 'Name the operator, UTC time, scope reference, vantage, and exact command before summarizing.', `EVIDENCE E-02\nUTC: 2026-08-26T15:14:09Z\nSCOPE: SOW-041\nVANTAGE: dc-ghost-operator\nCOMMAND: nmap -sV -Pn -p 8088 archive-target`, `A header that makes the artifact independently intelligible.`, 'A screenshot without provenance is decoration, not durable evidence.'),
      step('Preserve raw and derived copies', 'Hash the raw output and write interpretation in a separate note.', `sha256sum E-02.raw.txt > E-02.sha256`, `Hash file references the untouched raw artifact.`, 'Redactions belong in a derived copy. Never silently edit the original.'),
      step('Write a cheap reproduction', 'Give the minimum prerequisites, command, expected signal, and cleanup.', `Prerequisite: Ghost Port range active\nRun: curl -i http://archive-target:8088/status\nExpected: 200 and authentication:false\nCleanup: none; read-only request`, `A reviewer can repeat the validation in under a minute.`, 'Cheap reproduction shortens remediation and retest cycles.'),
    ],
    exercise: { brief: 'Repair a screenshot-only finding into a complete evidence item.', deliverable: 'Raw artifact, hash, provenance header, redacted copy, and reproduction note.', success: ['Raw material is unchanged', 'Secrets are absent from the shareable copy', 'Interpretation is visibly separate'] },
    references: [references.nist, references.wstg],
  },
  'finding-report': {
    level: 'FOUNDATION', environment: 'OFFLINE REPORT WORKBENCH',
    outcome: 'Turn technical evidence into a condition, consequence, remediation, and retest.',
    objectives: ['Write a tool-independent condition statement', 'Calibrate impact to reachable evidence', 'Specify a primary fix and closure test'],
    prerequisites: ['Evidence E-01 and E-02 from Ghost Port', 'Synthetic asset owner context'],
    steps: [
      step('Lead with the broken condition', 'Write what is wrong without naming the scanner.', `An undocumented Archive Console status service is reachable on dc-archive-02:8088 without authentication.`, `One sentence that remains true if the test tool changes.`, 'Avoid “Nmap found” and avoid severity adjectives in the condition.'),
      step('Connect access to consequence', 'Name who can reach it, what it reveals, and which assumptions remain.', `Any actor on the training subnet can retrieve service version and operational status. No write capability or sensitive records were observed.`, `Impact is bounded by actual evidence.`, 'Credibility increases when the report refuses to claim untested impact.'),
      step('Make closure executable', 'Give control, compensating control, and retest.', `Primary: remove the listener or require authenticated access.\nCompensating: restrict 8088 to the management segment.\nRetest: from the original vantage, confirm TCP access is denied or /status returns 401/403.`, `A fix owner can act and a tester can close.`, 'Remediation addresses the broken trust boundary, not the demonstration command.'),
    ],
    exercise: { brief: 'Write the full Ghost Port finding for a skeptical infrastructure owner.', deliverable: 'Title, condition, evidence, impact, primary fix, compensating control, and retest.', success: ['Every impact claim traces to evidence', 'Severity assumptions are named', 'Retest has an observable pass condition'] },
    references: [references.wstg, references.nist],
  },
  'os-attack-surface': {
    level: 'PRACTITIONER', environment: 'SYNTHETIC HOST ARTIFACTS',
    outcome: 'Map Windows and Linux privilege paths from configuration evidence.',
    objectives: ['Inventory identities, services, jobs, files, and management channels', 'Distinguish weak configuration from reachable privilege path', 'Recommend removal of the controlling edge'],
    prerequisites: ['Synthetic Windows service listing', 'Synthetic Linux permissions snapshot'],
    steps: [
      step('Inventory service trust', 'On a designated lab host, capture service identity, executable path, and start mode without changing state.', `Get-CimInstance Win32_Service | Select Name,StartName,PathName,StartMode`, `ArchiveAgent  LocalSystem  C:\\Program Files\\Archive\\agent.exe  Auto`, 'A privileged service matters only when an untrusted identity can influence its executable or configuration.'),
      step('Check the controlling file edge', 'Inspect the synthetic path ACL rather than assuming spaces or service identity are enough.', `Get-Acl 'C:\\Program Files\\Archive\\agent.exe' | Format-List`, `BUILTIN\\Users  Write`, 'User write access plus privileged execution creates a concrete path. Either fact alone is incomplete.'),
      step('Model the Linux equivalent', 'Review systemd unit, runtime identity, and writable dependencies on the lab artifact.', `systemctl cat archive-agent && namei -l /opt/archive/agent.sh`, `User=root\n...\ndrwxrwxr-x operators /opt/archive`, 'The permission edge is the finding; replacing a file is unnecessary to prove it.'),
    ],
    exercise: { brief: 'Analyze six host facts and identify which combinations create real privilege paths.', deliverable: 'A subject → controllable object → privileged execution graph.', success: ['No exploit is run', 'Every edge cites configuration evidence', 'Remediation removes the weakest controlling edge'] },
    references: [references.nist],
  },
  'web-surface-mapping': {
    level: 'PRACTITIONER', environment: 'SYNTHETIC HTTP CAPTURES',
    outcome: 'Build a web test map around identities, objects, transformations, and state changes.',
    objectives: ['Inventory routes together with purpose and trust decision', 'Trace input transformations', 'Create an account and object test matrix'],
    prerequisites: ['Browser developer tools', 'curl', 'Two approved synthetic roles'],
    steps: [
      step('Capture one complete transaction', 'Preserve method, path, content type, identity carrier, object identifier, and response status.', `curl -i -H "Authorization: Bearer $LAB_USER_TOKEN" https://lab.invalid/api/orders/1001`, `GET /api/orders/1001\nAuthorization: Bearer [REDACTED]\nHTTP/1.1 200 OK`, 'Record the token location but never place a live value into shared evidence.'),
      step('Map the decision', 'Annotate where authentication, object lookup, authorization, and serialization occur.', `session -> principal(user-17) -> order(1001) -> ownership check -> JSON serializer`, `A five-stage trust path.`, 'The test plan targets decisions, not a blind list of URLs.'),
      step('Build the matrix', 'Cross approved roles with owned, foreign synthetic, missing, and archived objects.', `anonymous/user/admin × own/foreign/missing/archived × GET/PATCH`, `A compact matrix of expected allow and deny outcomes.`, 'Negative tests are first-class requirements. A 403 can be the success signal.'),
    ],
    exercise: { brief: 'Map a synthetic order portal from six supplied HTTP captures.', deliverable: 'Route inventory, trust diagram, and twelve-cell access matrix.', success: ['Every route has a state-change label', 'Identity and object are named', 'Live tokens are redacted'] },
    references: [references.wstg, references.curl],
  },
  'auth-sessions': {
    level: 'PRACTITIONER', environment: 'SYNTHETIC IDENTITY LAB',
    outcome: 'Test the entire identity lifecycle with designated accounts and bounded attempts.',
    objectives: ['Trace enrollment through revocation', 'Test session invalidation after security events', 'Evaluate anti-automation without credential attacks'],
    prerequisites: ['Two synthetic accounts', 'Approved reset mailbox', 'No production identities'],
    steps: [
      step('Record the session baseline', 'Sign in once with a designated account and capture cookie attributes and server-side session identifiers with values redacted.', `Set-Cookie: dc_session=[REDACTED]; Secure; HttpOnly; SameSite=Lax`, `Transport, script access, and cross-site policy are visible.`, 'Cookie flags are important, but server-side lifetime and revocation still require testing.'),
      step('Exercise a state transition', 'Reset the synthetic account password, then replay only the pre-reset session once.', `curl -i -b 'dc_session=OLD_SYNTHETIC_SESSION' https://lab.invalid/profile`, `HTTP/1.1 401 Unauthorized`, 'The pass condition is invalidation. If access remains, stop after the single proof request.'),
      step('Test response consistency safely', 'Compare one valid-looking and one nonexistent synthetic username—never a harvested list.', `POST /login user=lab-alice ...\nPOST /login user=lab-never-exists ...`, `Both return the same status, message shape, and comparable timing.`, 'Enumeration testing needs only controlled comparison, not mass guessing.'),
    ],
    exercise: { brief: 'Assess login, logout, reset, privilege change, and suspension for one synthetic user.', deliverable: 'A lifecycle diagram with expected and observed session state after each event.', success: ['Attempts are bounded and documented', 'No production identity is touched', 'Revocation failures stop after proof'] },
    references: [references.wstg],
  },
  'authorization-testing': {
    level: 'PRACTITIONER', environment: 'SYNTHETIC MULTI-TENANT API',
    outcome: 'Prove or disprove server-side authorization with a minimal subject/action/object matrix.',
    objectives: ['Name subject, action, object, and tenant for each test', 'Compare owned and foreign synthetic objects', 'Stop at the first sufficient cross-boundary proof'],
    prerequisites: ['Tenant A user', 'Tenant B synthetic object', 'Approved read-only test'],
    steps: [
      step('Establish the allowed control', 'Request an object owned by the testing principal.', `curl -i -H "Authorization: Bearer $TENANT_A" https://lab.invalid/api/orders/A-100`, `HTTP/1.1 200 OK\n{"id":"A-100","tenant":"A"}`, 'A positive control proves the account and route work before the denial test.'),
      step('Change only the object', 'Keep identity, method, headers, and timing constant; substitute the designated foreign synthetic identifier.', `curl -i -H "Authorization: Bearer $TENANT_A" https://lab.invalid/api/orders/B-200`, `Expected: 403 or 404\nFailure signal: 200 with tenant B synthetic record`, 'Changing one variable makes the failed decision defensible.'),
      step('Test the write boundary without harm', 'Use a dry-run endpoint or reversible synthetic field if write testing is explicitly authorized.', `PATCH /api/orders/B-200 {"training_note":"DC-TEST"} with X-Lab-Dry-Run: true`, `Expected: deny before object mutation.`, 'Do not modify a real foreign record to make the finding more dramatic.'),
    ],
    exercise: { brief: 'Complete a six-cell access matrix across two roles and three synthetic objects.', deliverable: 'Request/response pairs plus the server-side decision expected for every cell.', success: ['A positive control is included', 'Only one variable changes per comparison', 'The proof contains no unrelated tenant data'] },
    references: [references.wstg],
  },
  'injection-validation': {
    level: 'PRACTITIONER', environment: 'OFFLINE QUERY WORKBENCH',
    outcome: 'Recognize interpreter boundaries and validate them with harmless synthetic proofs.',
    objectives: ['Identify where data becomes structure', 'Use reversible boolean or parser behavior', 'Recommend composition-safe APIs'],
    prerequisites: ['Synthetic application source snippet', 'Disposable local database'],
    steps: [
      step('Locate string composition', 'Trace the input from request to interpreter and mark the exact concatenation boundary.', `const sql = "SELECT id,name FROM lab_users WHERE id=" + req.query.id`, `Untrusted id becomes SQL syntax.`, 'The defect exists at composition; a payload is evidence, not the root cause.'),
      step('Use a non-destructive differential', 'In the disposable lab only, compare an expected-false condition with an expected-true condition.', `id=0 AND 1=2\nid=0 OR 1=1`, `Different row counts with stable status and no state change.`, 'A boolean differential can prove interpretation without modifying or exporting data.'),
      step('Repair the boundary', 'Replace concatenation with a parameterized query and repeat both inputs.', `db.query('SELECT id,name FROM lab_users WHERE id = ?', [req.query.id])`, `Both values are treated as data; malformed numeric input is rejected.`, 'Validation and parameterization solve different problems. Use both where the schema requires them.'),
    ],
    exercise: { brief: 'Review four synthetic sinks: SQL, shell, template, and HTML.', deliverable: 'For each: source, transformation, sink, harmless proof, and primary defense.', success: ['No destructive payloads', 'Context-specific defenses are named', 'Retest demonstrates data/structure separation'] },
    references: [references.wstg],
  },
  'api-security': {
    level: 'PRACTITIONER', environment: 'SYNTHETIC OPENAPI WORKBENCH',
    outcome: 'Turn an API contract into a systematic security test plan.',
    objectives: ['Extract endpoints, methods, schemas, and security requirements', 'Compare documented and observed operations', 'Bound resource-consumption tests'],
    prerequisites: ['Synthetic OpenAPI document', 'jq', 'curl'],
    steps: [
      step('Inventory the contract', 'Extract every path and method instead of browsing manually.', `jq -r '.paths | to_entries[] | .key as $p | .value | keys[] | "\\(.) \\($p)"' openapi.json`, `get /v1/orders\npost /v1/orders\nget /v1/orders/{id}`, 'The contract is a starting inventory. Client traffic and gateway routes may reveal drift.'),
      step('Compare supported methods', 'Use OPTIONS only on the approved synthetic endpoint and compare Allow with the contract.', `curl -i -X OPTIONS https://lab.invalid/v1/orders`, `Allow: GET, POST, DELETE`, 'An undocumented DELETE is a lead. Authorization and behavior still need controlled validation.'),
      step('Test limits with a ceiling', 'Use a tiny sequence around the documented page-size maximum, never an unbounded loop.', `for n in 99 100 101; do curl -s -o /dev/null -w "$n %{http_code} %{size_download}\n" "https://lab.invalid/v1/orders?limit=$n"; done`, `99 200 ...\n100 200 ...\n101 400 ...`, 'Three requests can validate boundary enforcement without turning the lesson into load testing.'),
    ],
    exercise: { brief: 'Audit a synthetic OpenAPI contract against eight captured requests.', deliverable: 'Endpoint delta, security requirement matrix, and three bounded negative tests.', success: ['Undocumented does not automatically mean vulnerable', 'Every test has a request ceiling', 'Schema and authorization failures are separated'] },
    references: [references.wstg, references.curl],
  },
  'identity-directory': {
    level: 'PRACTITIONER', environment: 'OFFLINE DIRECTORY EXPORT',
    outcome: 'Find consequential directory control paths without collecting credentials.',
    objectives: ['Map privileged membership and delegation', 'Separate direct rights from transitive paths', 'Recommend path reduction and tier isolation'],
    prerequisites: ['Sanitized directory export', 'PowerShell', 'No domain connectivity required'],
    steps: [
      step('Start with protected roles', 'Inventory membership and source for designated privileged groups in the sanitized export.', `Import-Csv groups.csv | Where-Object {$_.Group -in 'Domain Admins','Backup Operators'} | Select Group,Member,Source`, `Domain Admins  LAB\\dc-admin  direct\nBackup Operators LAB\\svc-backup nested`, 'Direct and nested membership need different remediation and ownership conversations.'),
      step('Expand one transitive path', 'Follow only the synthetic service account path through group nesting and delegated rights.', `svc-backup -> Backup Operators -> backup-agent ACL -> domain-controller backup data`, `A four-edge control path.`, 'The path matters because each edge is usable together. Avoid dumping unrelated directory data.'),
      step('Design the break point', 'Remove the lowest-cost unnecessary edge and add detection to the remaining sensitive transition.', `Replace standing membership with scheduled task identity; deny interactive sign-in; alert on group change.`, `Reduced standing privilege with an observable administrative path.`, 'Good remediation changes the graph, not just the account name.'),
    ],
    exercise: { brief: 'Analyze a sanitized graph containing two real privilege paths and four noisy relationships.', deliverable: 'Ranked paths with evidence, prerequisites, owner, and one break point each.', success: ['No password or hash material is used', 'Transitive edges are explicit', 'Remediation preserves the business function'] },
    references: [references.microsoft],
  },
  'cloud-control-plane': {
    level: 'PRACTITIONER', environment: 'OFFLINE IAM POLICY LAB',
    outcome: 'Calculate effective cloud access from request context and intersecting policy types.',
    objectives: ['Model principal, action, resource, and condition', 'Apply explicit-deny and boundary logic', 'Distinguish same-account from cross-account decisions'],
    prerequisites: ['Synthetic AWS policy set', 'jq', 'No cloud credentials required'],
    steps: [
      step('Normalize the request', 'Write the exact principal, action, resource, and environmental conditions before reading policies.', `Principal: arn:aws:iam::111122223333:role/LabAnalyst\nAction: s3:GetObject\nResource: arn:aws:s3:::dc-training/reports/a.txt\nCondition: aws:SourceIp=198.51.100.8`, `One request context, not a vague “bucket access” question.`, 'Authorization decisions evaluate requests, not whole identities in the abstract.'),
      step('Evaluate denies first', 'Search applicable identity, resource, boundary, session, and organization policies for an explicit deny.', `jq '.. | objects | select(.Effect? == "Deny")' policies.json`, `Deny when aws:SecureTransport is false`, 'An applicable explicit deny overrides an allow. If it does not match the request, evaluation continues.'),
      step('Compute the intersections', 'Apply union for identity/resource grants and intersection for boundaries and organization controls.', `grant = identity_allow ∪ resource_allow\neffective = grant ∩ boundary ∩ SCP\nexplicit_deny => DENY`, `Decision: ALLOW only when every limiting layer permits the request.`, 'Preserve the policy statements that contributed to the decision so another reviewer can reproduce it.'),
    ],
    exercise: { brief: 'Decide eight synthetic IAM requests across same-account and cross-account cases.', deliverable: 'For each: context, applicable policies, deny/allow path, final decision, and confidence.', success: ['Explicit deny is evaluated correctly', 'Boundaries never grant access', 'Cross-account requirements are not treated like same-account union'] },
    references: [references.aws],
  },
  'containers-supply-chain': {
    level: 'PRACTITIONER', environment: 'OFFLINE KUBERNETES MANIFEST LAB',
    outcome: 'Review workload isolation and artifact provenance from manifests and metadata.',
    objectives: ['Identify runtime privilege and host interaction', 'Trace image identity and provenance', 'Prioritize reachable dependency risk'],
    prerequisites: ['Synthetic deployment YAML', 'kubectl client or YAML parser', 'No cluster required'],
    steps: [
      step('Interrogate the security context', 'Extract identity, privilege escalation, capabilities, filesystem mode, and host namespaces.', `kubectl apply --dry-run=client -f deployment.yaml -o json | jq '.spec.template.spec | {hostNetwork,hostPID,containers:[.containers[]|{name,securityContext}]}'`, `runAsNonRoot:false\nallowPrivilegeEscalation:true\nreadOnlyRootFilesystem:false`, 'Each setting is evidence. Risk depends on the workload and surrounding controls.'),
      step('Find host-boundary crossings', 'List hostPath mounts, privileged containers, and service account use.', `grep -nE 'hostPath|privileged:|serviceAccountName' deployment.yaml`, `42: hostPath:\n18: serviceAccountName: archive-writer`, 'Host mounts and powerful workload identity can connect container compromise to wider control.'),
      step('Pin the artifact', 'Replace mutable tags with a reviewed digest and record provenance evidence.', `image: registry.invalid/archive@sha256:4f2c...`, `Deployment references one immutable image digest.`, 'A digest identifies bytes; provenance and signature evidence explain where those bytes came from.'),
    ],
    exercise: { brief: 'Review three manifests and one synthetic SBOM.', deliverable: 'Isolation findings, identity paths, digest inventory, and reachable dependency triage.', success: ['Checklist observations are contextualized', 'Mutable tags are identified', 'Dependency findings include runtime reachability'] },
    references: [references.kubernetes],
  },
  'secrets-credential-audit': {
    level: 'PRACTITIONER', environment: 'OFFLINE SYNTHETIC CREDENTIAL LAB',
    outcome: 'Audit secret lifecycle and password controls without online guessing.',
    objectives: ['Map creation through revocation', 'Evaluate policy with synthetic or sanctioned offline data', 'Prove old material stops working after rotation'],
    prerequisites: ['Synthetic secret inventory', 'Synthetic password policy export', 'No live credentials'],
    steps: [
      step('Build the lifecycle record', 'For each synthetic secret, capture owner, purpose, storage, distribution, rotation, revocation, and logging.', `id: lab-api-01\nowner: Platform Lab\nstorage: secret manager\nrotation: 90d\nlast_rotated: 142d\nconsumers: [build-7, api-lab]`, `One stale synthetic secret with two known consumers.`, 'Strength does not compensate for unknown copies or missing revocation.'),
      step('Check policy offline', 'Compare a sanctioned synthetic dataset with length, known-compromise, and reuse controls—never authenticate against accounts.', `policy-check --input synthetic-passwords.txt --min-length 14 --blocklist lab-blocklist.txt`, `12 checked; 3 too short; 2 blocklisted; 1 reused`, 'The output measures policy behavior without lockouts, account discovery, or service load.'),
      step('Verify rotation end to end', 'After rotating the lab secret, test one approved old token and one new token against the synthetic service.', `old token -> 401\nnew token -> 200\nold build artifact -> no embedded token`, `Revocation is proven across known consumers.`, 'Rotation is incomplete when old sessions, artifacts, or downstream copies still authorize access.'),
    ],
    exercise: { brief: 'Audit five synthetic secrets and a sanctioned password sample.', deliverable: 'Lifecycle gaps, offline policy results, rotation plan, and revocation proof.', success: ['No network guessing occurs', 'No real secret enters the artifact', 'Every consumer is included in rotation'] },
    references: [references.nist],
  },
  'vulnerability-validation': {
    level: 'PRACTITIONER', environment: 'OFFLINE ADVISORY WORKBENCH',
    outcome: 'Convert a scanner lead into a validated, environmentally prioritized finding.',
    objectives: ['Confirm product, version, configuration, and reachability', 'Separate advisory severity from operational risk', 'Document compensating controls and uncertainty'],
    prerequisites: ['Synthetic scanner result', 'Synthetic SBOM', 'Vendor advisory copy'],
    steps: [
      step('Match the exact affected condition', 'Compare package identity, version range, feature flag, and platform with the vendor advisory.', `observed: archive-parser 2.4.1\naffected: >=2.3.0 <2.4.3\nrequired feature: remote-import=true\nobserved feature: false`, `Version matches; required feature is disabled.`, 'The scanner lead is real inventory but the vulnerable path is not currently reachable.'),
      step('Validate reachability safely', 'Trace whether untrusted input can reach the affected component without triggering exploit behavior.', `public upload -> type allowlist -> parser dispatch\nremote-import route -> disabled`, `No authorized input path reaches remote-import.`, 'Reachability analysis can reduce uncertainty without running exploit code.'),
      step('Prioritize with environment', 'Combine exposure, prerequisites, asset role, detection, recovery, and known exploitation status.', `Technical severity: high\nCurrent reachability: not observed\nAsset criticality: medium\nCompensating control: feature disabled\nAction: patch in normal window; monitor config drift`, `A defensible operational priority.`, 'A severe advisory can still warrant prompt patching; validated context determines response order, not whether the defect exists.'),
    ],
    exercise: { brief: 'Triage four synthetic scanner alerts, including one known-exploited issue.', deliverable: 'Evidence-backed disposition and remediation priority for each.', success: ['False positive and not-reachable are not confused', 'Vendor conditions are cited', 'Known exploitation changes urgency explicitly'] },
    references: [references.cisa, references.nist],
  },
  'impact-cleanup': {
    level: 'PRACTITIONER', environment: 'OFFLINE ENGAGEMENT WORKBENCH',
    outcome: 'Define an impact ceiling, track test artifacts, and execute a meaningful retest.',
    objectives: ['Translate authorization into an impact ceiling', 'Maintain a complete cleanup ledger', 'Retest the original control and adjacent regression risk'],
    prerequisites: ['Synthetic finding', 'Approved remediation note', 'No production access'],
    steps: [
      step('Write the proof ceiling', 'State exactly what observable result proves impact and which follow-on actions remain prohibited.', `Proof: synthetic Tenant B order title is returned to Tenant A test user.\nStop before: downloading attachments, enumerating IDs, modifying records, or testing real tenants.`, `A sufficient proof with four explicit non-actions.`, 'Impact discipline is planned before the exciting result appears.'),
      step('Track every artifact', 'Record accounts, files, tokens, callbacks, jobs, records, and infrastructure at creation time.', `artifact: training_note DC-TEST-8421\ncreated: 15:21Z\nowner: OP-07\ncleanup: DELETE synthetic note\nverified: pending`, `A ledger entry that cannot be forgotten during teardown.`, 'Cleanup is evidence-producing work, not a final memory exercise.'),
      step('Retest the decision', 'Repeat the minimal original request after remediation and inspect the intended denial.', `Tenant A token -> GET /api/orders/B-200`, `HTTP/1.1 403 Forbidden\nrequest_id: retest-044`, 'Also confirm Tenant B still accesses its own record. A fix that denies everyone is an outage, not correct authorization.'),
    ],
    exercise: { brief: 'Close a synthetic cross-tenant finding from proof ceiling through cleanup and retest.', deliverable: 'Impact plan, cleanup ledger with verified status, negative retest, and positive regression control.', success: ['No action exceeds the proof ceiling', 'Every artifact is verified removed', 'Authorized behavior still works after the fix'] },
    references: [references.nist, references.wstg],
  },
}
