const specs = [
  { id:'http-transaction-evidence', title:'HTTP transactions as evidence', lab:'reflected-context', focus:'HTTP is a stateful conversation assembled from method, authority, path, headers, body, connection behavior, redirects, cookies, and intermediary transformations.', boundary:'A useful capture preserves both sides of one transaction while redacting live credentials and separating browser behavior from server enforcement.', signal:'two requests that differ by one controlled variable and retain raw status, headers, and body', control:'reproduce the decision with a minimal request and document every intermediary that can rewrite it', artifact:'a redacted request-response pair with a one-variable comparison', request:`curl -i 'http://lab.invalid/search?q=operator'`, compare:`curl -i 'http://lab.invalid/search?q=%3Cprobe%3E'`, prevent:'Record raw bytes, decoded meaning, identity context, and the conclusion separately.', refs:['https://developer.mozilla.org/en-US/docs/Web/HTTP','https://portswigger.net/web-security/learning-paths'] },
  { id:'attack-surface-state-map', title:'Stateful attack-surface mapping', lab:'session-rotation', focus:'Routes matter only when connected to identities, objects, state changes, asynchronous workers, parsers, and external callbacks.', boundary:'The map must include anonymous, ordinary, privileged, suspended, and cross-tenant states without enumerating unapproved customer data.', signal:'a route-state matrix showing which principal can trigger each protected transition', control:'derive the surface from documentation, client behavior, schemas, traffic, and approved role comparisons', artifact:'an attack-surface graph with entry point, trust decision, state mutation, and owner', request:`curl -s http://lab.invalid/map | jq`, compare:`curl -i -H 'Authorization: Bearer LAB_USER' http://lab.invalid/account`, prevent:'Require an explicit server-side owner and authorization decision for every state-changing operation.', refs:['https://owasp.org/www-project-web-security-testing-guide/','https://portswigger.net/web-security/mapping-the-attack-surface'] },
  { id:'xss-output-contexts', title:'Cross-site scripting by output context', lab:'reflected-context', focus:'Cross-site scripting is an output-context failure: the same bytes have different meaning in HTML text, attributes, URLs, JavaScript strings, CSS, and DOM sinks.', boundary:'Use inert markers and the smallest approved browser proof; never collect cookies, credentials, or data from another user.', signal:'a marker crosses from data into executable browser syntax in one precisely named context', control:'apply context-specific output encoding and avoid unsafe sinks rather than filtering a blacklist of characters', artifact:'a source-to-sink trace naming parser transitions and the correct encoder', request:`curl -s 'http://lab.invalid/search?q=DC_MARKER'`, compare:`curl -s 'http://lab.invalid/search?q=%3Csvg%20data-dc%3D1%3E'`, prevent:'Encode for the final output context and use safe DOM APIs such as textContent.', refs:['https://owasp.org/www-community/attacks/xss/','https://portswigger.net/web-security/cross-site-scripting'] },
  { id:'dom-xss-dataflow', title:'DOM dataflow and client-side sinks', lab:'stored-context', focus:'DOM XSS appears when browser-controlled sources flow through decoding and framework transformations into a sink that interprets data as markup or code.', boundary:'Trace only the designated synthetic value through the supplied client bundle and stop after an inert sink transition is proven.', signal:'a reproducible source-transform-sink chain with the exact final interpretation', control:'replace interpreting sinks, constrain URL schemes, and validate after the last decoding step', artifact:'a client-side dataflow diagram with source, transforms, sink, and safe replacement', request:`curl -s http://lab.invalid/assets/profile.js`, compare:`curl -s 'http://lab.invalid/profile#DC_MARKER'`, prevent:'Use textContent or structured rendering and reject dangerous schemes at the consumption point.', refs:['https://owasp.org/www-community/attacks/DOM_Based_XSS','https://portswigger.net/web-security/dom-based'] },
  { id:'csrf-request-authority', title:'CSRF and request authority', lab:'csrf-origin', focus:'Cross-site request forgery succeeds when a browser automatically supplies ambient authority to a state-changing request that the server cannot distinguish from user intent.', boundary:'Use a synthetic account and reversible preference change; do not test financial, administrative, or destructive actions.', signal:'a cross-site request changes state without a per-request secret or trustworthy origin decision', control:'combine SameSite cookies, unpredictable CSRF tokens, origin validation, and reauthentication for sensitive actions', artifact:'a request-authority table covering cookie behavior, token binding, origin, and method', request:`curl -i -b 'session=LAB' http://lab.invalid/preferences`, compare:`curl -i -X POST -b 'session=LAB' -H 'Origin: https://foreign.invalid' http://lab.invalid/preferences`, prevent:'Require a session-bound anti-CSRF token and verify Origin on state-changing requests.', refs:['https://owasp.org/www-community/attacks/csrf','https://portswigger.net/web-security/csrf'] },
  { id:'cors-origin-decisions', title:'CORS origin decisions', lab:'cors-reflection', focus:'CORS is a browser read-permission policy, not server authentication. Risk appears when sensitive responses are shared with an origin that should not receive them.', boundary:'Test only designated synthetic origins and records, recording preflight and actual-response behavior independently.', signal:'a sensitive response reflects or broadly permits an untrusted Origin while credentials are accepted', control:'match exact trusted origins, vary responses correctly, and never combine credentialed access with broad origin trust', artifact:'an origin matrix with credential mode, ACAO, ACAC, preflight, and readable outcome', request:`curl -i -H 'Origin: https://app.lab.invalid' http://lab.invalid/profile`, compare:`curl -i -H 'Origin: https://app.lab.invalid.attacker.invalid' http://lab.invalid/profile`, prevent:'Use an exact allowlist and emit Vary: Origin for dynamic decisions.', refs:['https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS','https://portswigger.net/web-security/cors'] },
  { id:'csp-browser-boundaries', title:'CSP and browser containment', lab:'stored-context', focus:'Content Security Policy limits which browser resources and execution paths are trusted, but it does not repair unsafe rendering or server authorization.', boundary:'Evaluate the delivered policy and designated page without attempting third-party callbacks or data collection.', signal:'the policy permits a concrete execution path through an allowed host, nonce mistake, unsafe directive, or missing boundary', control:'deploy a restrictive nonce- or hash-based policy, remove unsafe sinks, and monitor reports before enforcement', artifact:'a directive-by-directive policy review tied to actual page resources', request:`curl -sI http://lab.invalid/profile`, compare:`curl -s http://lab.invalid/profile | grep -E 'script|iframe|object'`, prevent:'Use nonces or hashes, object-src none, base-uri none, and explicit frame ancestry.', refs:['https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP','https://portswigger.net/web-security/cross-site-scripting/content-security-policy'] },
  { id:'sql-injection-boundaries', title:'SQL injection and query structure', lab:'sql-parameter', focus:'SQL injection occurs when untrusted input becomes query structure instead of a typed parameter, often after concatenation, dynamic identifiers, or unsafe query builders.', boundary:'Use the seeded synthetic catalog and boolean comparisons; never dump unrelated data or modify records.', signal:'a controlled input changes query logic while the application identity and database state remain fixed', control:'parameterize values, allowlist identifiers, reduce database privilege, and test the final query-building layer', artifact:'a query construction trace with parameter boundary and least-privilege review', request:`curl -s 'http://lab.invalid/catalog?category=books'`, compare:`curl -sG --data-urlencode "category=books' OR '1'='1" http://lab.invalid/catalog`, prevent:'Use prepared statements for values and fixed mappings for dynamic identifiers.', refs:['https://owasp.org/www-community/attacks/SQL_Injection','https://portswigger.net/web-security/sql-injection'] },
  { id:'nosql-operator-injection', title:'NoSQL operator injection', lab:'nosql-operator', focus:'Document databases and query DSLs become injectable when structured operators, type coercion, or nested objects cross an untrusted JSON boundary.', boundary:'Use synthetic identities and an allowlisted comparison that proves query-shape manipulation without accessing additional records.', signal:'a scalar field is accepted as an operator-bearing object and changes the match predicate', control:'enforce strict schemas, reject operator keys, construct queries explicitly, and use database roles with narrow access', artifact:'a JSON type map showing accepted shape, dangerous operator, and corrected schema', request:`curl -s -H 'Content-Type: application/json' -d '{"user":"lab-alice"}' http://lab.invalid/lookup`, compare:`curl -s -H 'Content-Type: application/json' -d '{"user":{"$ne":""}}' http://lab.invalid/lookup`, prevent:'Validate primitive types before query construction and reject keys beginning with operator prefixes.', refs:['https://owasp.org/www-project-web-security-testing-guide/','https://portswigger.net/web-security/nosql-injection'] },
  { id:'command-injection-processes', title:'Command injection and process boundaries', lab:'command-argv', focus:'Command injection appears when data crosses into shell grammar or an unsafe process invocation, allowing separators, substitutions, options, or environment behavior to alter execution.', boundary:'Use an inert marker and designated diagnostic command; never establish persistence, access external networks, or alter the container.', signal:'input changes the process argument structure or causes an additional harmless command to execute', control:'avoid the shell, pass fixed executable arguments, terminate options, validate domain types, and minimize process privilege', artifact:'a process invocation model with executable, argv, environment, identity, and safe API', request:`curl -s 'http://lab.invalid/diagnose?host=web-target'`, compare:`curl -sG --data-urlencode 'host=web-target;printf DC_MARKER' http://lab.invalid/diagnose`, prevent:'Invoke the fixed binary with an argument array and validate the host as a hostname or IP value.', refs:['https://owasp.org/www-community/attacks/Command_Injection','https://portswigger.net/web-security/os-command-injection'] },
  { id:'path-canonicalization', title:'Path traversal and canonicalization', lab:'path-canonical', focus:'Path traversal is a namespace failure: encoded separators, normalization order, symlinks, and platform semantics can move a resolved path outside the intended root.', boundary:'Request only the provided synthetic files and designated canary; do not enumerate the container filesystem.', signal:'the final canonical path escapes the allowed root and returns the designated canary', control:'map opaque identifiers to server-side paths, canonicalize once, verify containment, and run with minimal file access', artifact:'a decode-normalize-resolve timeline with root-containment proof', request:`curl -s 'http://lab.invalid/files?name=guide.txt'`, compare:`curl -sG --data-urlencode 'name=../canary.txt' http://lab.invalid/files`, prevent:'Resolve against a fixed root and reject any final path that is not contained within it.', refs:['https://owasp.org/www-community/attacks/Path_Traversal','https://portswigger.net/web-security/file-path-traversal'] },
  { id:'file-upload-pipelines', title:'File-upload trust pipelines', lab:'upload-trust', focus:'Upload security spans parsing, naming, storage, scanning, metadata, rendering, authorization, and downstream processors—not merely a filename extension.', boundary:'Use supplied inert fixtures and a non-executable marker; never upload malware or active content to a non-lab system.', signal:'server-side behavior trusts caller-controlled type, name, or location across a dangerous processing boundary', control:'generate names, store outside execution roots, validate content, re-encode where possible, and isolate processors', artifact:'an upload lifecycle showing every parser, storage boundary, and retrieval authorization decision', request:`curl -s -F 'file=@fixtures/avatar.png;type=image/png' http://lab.invalid/upload`, compare:`curl -s -F 'file=@fixtures/marker.txt;filename=avatar.png;type=image/png' http://lab.invalid/upload`, prevent:'Validate decoded content, generate a storage key, and serve through a non-executable isolated origin.', refs:['https://owasp.org/www-community/vulnerabilities/Unrestricted_File_Upload','https://portswigger.net/web-security/file-upload'] },
  { id:'xxe-parser-capabilities', title:'XXE and parser capabilities', lab:'xxe-parser', focus:'XML external entity risk depends on parser capabilities, entity resolution, network and file access, transformation features, and the identity running the parser.', boundary:'Use the provided local canary URI only; external callbacks and arbitrary file reads are outside the exercise.', signal:'the parser resolves a designated external entity and incorporates the canary into its output', control:'disable DTDs and external entities, use hardened libraries, isolate transforms, and remove unnecessary parser features', artifact:'a parser capability inventory with configuration evidence and negative test', request:`curl -s -H 'Content-Type: application/xml' --data '<order><id>7</id></order>' http://lab.invalid/xml`, compare:`curl -s -H 'Content-Type: application/xml' --data-binary @fixtures/canary.xml http://lab.invalid/xml`, prevent:'Disable external entity resolution and verify the canary remains unresolved.', refs:['https://owasp.org/www-community/vulnerabilities/XML_External_Entity_(XXE)_Processing','https://portswigger.net/web-security/xxe'] },
  { id:'ssti-template-context', title:'Server-side template injection', lab:'template-expression', focus:'Template injection occurs when untrusted input becomes template source or reaches an evaluation primitive rather than remaining a value in a fixed template.', boundary:'Use arithmetic and string markers only; do not access environment variables, files, processes, or network functions.', signal:'the server evaluates a harmless expression supplied through the designated field', control:'keep templates static, pass data as values, sandbox only as defense in depth, and minimize renderer privilege', artifact:'a template dataflow showing compilation point, context objects, and fixed-template repair', request:`curl -sG --data-urlencode 'name=Operator' http://lab.invalid/welcome`, compare:`curl -sG --data-urlencode 'name={{7*7}}' http://lab.invalid/welcome`, prevent:'Compile trusted templates at build time and bind user input only as data.', refs:['https://owasp.org/www-project-web-security-testing-guide/','https://portswigger.net/web-security/server-side-template-injection'] },
  { id:'deserialization-type-trust', title:'Deserialization and type trust', lab:'typed-object', focus:'Unsafe deserialization lets untrusted data select types, constructors, callbacks, or object graphs that were never intended as external input.', boundary:'Use the supplied harmless type marker and stop when unexpected type selection is proven; do not invoke system functions.', signal:'a caller-controlled type discriminator constructs an unapproved class or triggers a designated callback', control:'use simple schemas, explicit type allowlists, integrity protection, and process isolation for unavoidable legacy formats', artifact:'a format-to-object map with permitted types, side effects, and replacement schema', request:`curl -s -H 'Content-Type: application/json' -d '{"type":"Note","text":"hello"}' http://lab.invalid/object`, compare:`curl -s -H 'Content-Type: application/json' -d '{"type":"DebugMarker","text":"hello"}' http://lab.invalid/object`, prevent:'Deserialize into inert data structures and map explicit allowed variants without dynamic construction.', refs:['https://owasp.org/www-community/vulnerabilities/Deserialization_of_untrusted_data','https://portswigger.net/web-security/deserialization'] },
  { id:'ssrf-destination-control', title:'SSRF and destination control', lab:'ssrf-allowlist', focus:'Server-side request forgery crosses a network trust boundary when a caller influences destination, scheme, redirect, resolver result, or proxy behavior of a server-side request.', boundary:'Use only the supplied public mock and internal canary services inside the sealed range; no external or host destinations are permitted.', signal:'the fetcher reaches the designated internal canary through a caller-controlled destination or redirect', control:'use fixed destination mappings, resolve and revalidate addresses, block private ranges, constrain schemes, and isolate egress', artifact:'a destination decision tree covering parsing, DNS, redirects, proxying, and network enforcement', request:`curl -sG --data-urlencode 'url=http://public-mock:8080/status' http://lab.invalid/fetch`, compare:`curl -sG --data-urlencode 'url=http://internal-canary:8080/status' http://lab.invalid/fetch`, prevent:'Map approved service identifiers to fixed destinations and enforce network egress policy.', refs:['https://owasp.org/www-community/attacks/Server_Side_Request_Forgery','https://portswigger.net/web-security/ssrf'] },
  { id:'authentication-lifecycle', title:'Authentication lifecycle failures', lab:'session-rotation', focus:'Authentication is a lifecycle spanning enrollment, login, MFA, recovery, step-up, device trust, suspension, and closure; the weakest transition defines assurance.', boundary:'Use designated synthetic identities and a single comparison per state transition; do not guess credentials or enumerate accounts.', signal:'a lifecycle transition grants or retains authority without satisfying the intended assurance requirement', control:'bind every transition to an explicit assurance policy, rate limit safely, and make recovery at least as strong as login', artifact:'an identity-state machine with assurance level, token effect, and required revocation', request:`curl -s -H 'Authorization: Bearer SESSION_OLD' http://lab.invalid/me`, compare:`curl -s -X POST -H 'Authorization: Bearer RECOVERY' http://lab.invalid/reset`, prevent:'Invalidate affected sessions and require the intended assurance level at every recovery transition.', refs:['https://owasp.org/www-project-application-security-verification-standard/','https://portswigger.net/web-security/authentication'] },
  { id:'session-token-rotation', title:'Session rotation and revocation', lab:'session-rotation', focus:'Session security depends on unpredictable identifiers, secure transport, rotation at privilege changes, bounded lifetime, server-side revocation, and correct cookie scope.', boundary:'Replay only the designated old synthetic session once after the approved transition and stop after pass or failure.', signal:'an old session remains authorized after password reset, logout, privilege reduction, or administrative suspension', control:'rotate identifiers after authentication, revoke related sessions on sensitive transitions, and scope cookies narrowly', artifact:'a before-and-after session ledger with transition, expected invalidation, and observed result', request:`curl -i -b 'dc_session=SESSION_OLD' http://lab.invalid/me`, compare:`curl -i -X POST -H 'Authorization: Bearer RECOVERY' http://lab.invalid/reset && curl -i -b 'dc_session=SESSION_OLD' http://lab.invalid/me`, prevent:'Maintain server-side session state and revoke every affected token at the transition boundary.', refs:['https://owasp.org/www-project-cheat-sheets/cheatsheets/Session_Management_Cheat_Sheet.html','https://portswigger.net/web-security/authentication'] },
  { id:'object-authorization', title:'Object authorization and BOLA', lab:'bola-object', focus:'Broken object-level authorization occurs when a valid identity can perform an action on an object that the server has not proven belongs to its allowed subject, tenant, or role.', boundary:'Compare one owned object with one explicitly designated foreign synthetic object; never enumerate identifiers.', signal:'the same low-privilege identity receives the foreign object while all other request properties remain fixed', control:'derive tenant and ownership server-side, authorize every object operation, and avoid trusting caller-supplied scope', artifact:'a subject-action-object matrix with positive control, negative control, and server decision evidence', request:`curl -s -H 'Authorization: Bearer TENANT_A' http://lab.invalid/orders/A-100`, compare:`curl -s -H 'Authorization: Bearer TENANT_A' http://lab.invalid/orders/B-200`, prevent:'Resolve the object and evaluate ownership or tenant membership before returning any representation.', refs:['https://owasp.org/API-Security/editions/2023/en/0xa1-broken-object-level-authorization/','https://portswigger.net/web-security/access-control/idor'] },
  { id:'jwt-verification', title:'JWT verification and key selection', lab:'jwt-algorithm', focus:'JWT security depends on trusted algorithm selection, issuer and audience binding, time validation, key provenance, and authorization after signature verification.', boundary:'Use only the supplied synthetic tokens and lab public key; do not forge tokens for external services.', signal:'the verifier accepts a token under a caller-influenced algorithm, key identifier, issuer, or audience decision', control:'configure allowed algorithms and keys server-side, validate all required claims, and treat token contents as untrusted until verification completes', artifact:'a verification pipeline naming trusted configuration, claims, key source, and rejection cases', request:`curl -s -H 'Authorization: Bearer GOOD_LAB_JWT' http://lab.invalid/admin`, compare:`curl -s -H 'Authorization: Bearer ALG_CONFUSION_LAB_JWT' http://lab.invalid/admin`, prevent:'Pin the asymmetric algorithm and issuer key set; never derive verification mode from the token header alone.', refs:['https://owasp.org/www-project-cheat-sheets/cheatsheets/JSON_Web_Token_for_Java_Cheat_Sheet.html','https://portswigger.net/web-security/jwt'] },
  { id:'oauth-redirect-binding', title:'OAuth redirect and state binding', lab:'oauth-redirect', focus:'OAuth security depends on exact redirect binding, state correlation, authorization-code ownership, PKCE, issuer validation, and separation of clients and flows.', boundary:'Use the synthetic authorization server and registered lab clients; no external redirect or real identity provider is involved.', signal:'a code or token can be delivered to an unregistered redirect or attached to the wrong browser transaction', control:'match exact redirect URIs, require state and PKCE, bind codes to client and redirect, and validate issuer', artifact:'a complete authorization flow diagram with trust decisions and one negative control per transition', request:`curl -si 'http://lab.invalid/oauth/authorize?client_id=academy&redirect_uri=https://academy.lab/callback&state=DC1'`, compare:`curl -si 'http://lab.invalid/oauth/authorize?client_id=academy&redirect_uri=https://academy.lab.attacker.invalid/callback&state=DC1'`, prevent:'Register exact redirect URIs and bind the authorization code to client, redirect URI, and PKCE verifier.', refs:['https://www.rfc-editor.org/rfc/rfc9700','https://portswigger.net/web-security/oauth'] },
  { id:'graphql-field-controls', title:'GraphQL field authorization', lab:'graphql-field', focus:'GraphQL exposes a flexible query language over a graph; authorization must hold at resolver, object, field, mutation, and batching boundaries rather than only at the endpoint.', boundary:'Use the provided schema and designated tenant records; keep depth and aliases within the lab limit.', signal:'a low-privilege query resolves a protected field or foreign node through an alternate graph path', control:'authorize inside resolvers, constrain schema exposure and query cost, and test every path to sensitive fields', artifact:'a schema authorization map with field owner, resolver decision, cost, and negative query', request:`curl -s -H 'Content-Type: application/json' -d '{"query":"{ me { id name } }"}' http://lab.invalid/graphql`, compare:`curl -s -H 'Content-Type: application/json' -d '{"query":"{ me { id billingToken } }"}' http://lab.invalid/graphql`, prevent:'Enforce field-level authorization in the resolver using the authenticated principal and resolved object.', refs:['https://owasp.org/www-project-graphql-security/','https://portswigger.net/web-security/graphql'] },
  { id:'business-logic-invariants', title:'Business-logic invariants', lab:'race-atomicity', focus:'Business-logic flaws violate a workflow invariant even when every request is syntactically valid, authenticated, and individually authorized.', boundary:'Use the reversible synthetic order and declared value ceiling; stop when one invariant violation is proven.', signal:'a permitted sequence, ordering, value, or state transition produces an outcome the business rules forbid', control:'define invariants explicitly, enforce them server-side at the authoritative transaction boundary, and log rejected transitions', artifact:'a workflow state machine with invariant, precondition, transition, and abuse case', request:`curl -s -H 'Authorization: Bearer BUYER' http://lab.invalid/order/7`, compare:`curl -s -X POST -H 'Authorization: Bearer BUYER' -d 'quantity=-1' http://lab.invalid/order/7/update`, prevent:'Validate the business invariant against authoritative state inside the same transaction that commits the change.', refs:['https://owasp.org/www-project-web-security-testing-guide/','https://portswigger.net/web-security/logic-flaws'] },
  { id:'race-condition-atomicity', title:'Race conditions and atomicity', lab:'race-atomicity', focus:'Race conditions appear when correctness depends on timing between a check and a state change, allowing concurrent requests to violate a single-use or balance invariant.', boundary:'Use the range-provided concurrency harness, maximum two requests, and synthetic coupon; never load-test an external service.', signal:'two concurrent authorized requests both pass a stale check and commit an outcome that should be single-use', control:'enforce the invariant with atomic database operations, transactions, unique constraints, idempotency keys, or locking', artifact:'a synchronized request timeline with check, commit, resulting state, and atomic repair', request:`curl -s -X POST -H 'Idempotency-Key: DC-1' http://lab.invalid/coupon/apply`, compare:`dc-race http://lab.invalid/coupon/apply 2`, prevent:'Move the single-use check and state mutation into one atomic database operation.', refs:['https://portswigger.net/web-security/race-conditions','https://owasp.org/www-project-web-security-testing-guide/'] },
  { id:'request-smuggling-framing', title:'HTTP request framing and desynchronization', lab:'framing-disagreement', focus:'Request smuggling arises when adjacent HTTP components disagree about message boundaries, transfer encoding, content length, or connection reuse.', boundary:'Use the purpose-built two-parser simulator only; never send ambiguous framing to an external or shared service.', signal:'the front and back parser consume different byte boundaries for the same synthetic stream', control:'normalize at one trusted layer, reject ambiguous messages, align parser versions, and avoid unsafe protocol downgrades', artifact:'a byte-level framing diagram with each parser decision and rejection policy', request:`printf 'POST / HTTP/1.1\r\nContent-Length: 4\r\n\r\nTEST' | dc-frame`, compare:`cat fixtures/ambiguous-request.txt | dc-frame`, prevent:'Reject requests containing conflicting framing signals and keep frontend/backend parsing behavior aligned.', refs:['https://portswigger.net/web-security/request-smuggling','https://owasp.org/www-project-web-security-testing-guide/'] },
  { id:'cache-key-integrity', title:'Cache keys, poisoning, and deception', lab:'cache-key', focus:'Web cache risk appears when the cache key omits an input that changes the response, or when routing and caching disagree about whether sensitive content is public.', boundary:'Use the isolated cache simulator and synthetic response only; do not poison a shared or production cache.', signal:'two security-distinct requests map to one cache key or a private response becomes cacheable under a public-looking path', control:'include every response-varying input, define explicit cacheability, normalize consistently, and separate authenticated content', artifact:'a cache-key inventory with unkeyed inputs, response influence, TTL, and purge plan', request:`curl -si -H 'X-Forwarded-Host: academy.lab' http://lab.invalid/cache/home`, compare:`curl -si -H 'X-Forwarded-Host: attacker.invalid' http://lab.invalid/cache/home`, prevent:'Exclude untrusted host metadata from response construction or include validated variants in the key.', refs:['https://portswigger.net/web-security/web-cache-poisoning','https://portswigger.net/web-security/web-cache-deception'] },
  { id:'api-resource-controls', title:'API resource and abuse controls', lab:'resource-budget', focus:'API availability controls must bound pagination, batching, query depth, upload size, compute cost, retries, and concurrency without relying on one global request counter.', boundary:'Run only the fixed-budget synthetic workload inside the sealed range and honor automatic stop thresholds.', signal:'one low-rate request shape consumes disproportionate work or bypasses a cost, size, or concurrency limit', control:'budget by identity and operation cost, cap expensive dimensions, isolate workers, and return predictable backpressure', artifact:'an operation-cost model with limit, measurement, abort condition, and recovery evidence', request:`curl -s 'http://lab.invalid/report?rows=10'`, compare:`curl -s 'http://lab.invalid/report?rows=100000&expand=all'`, prevent:'Apply server-side cost budgets and hard caps before allocating work.', refs:['https://owasp.org/API-Security/editions/2023/en/0xa4-unrestricted-resource-consumption/','https://www.rfc-editor.org/rfc/rfc6585'] },
]

function makeInteractive(spec) {
  return {
    title: `${spec.title} decision room`, mode: 'WEB/API EVIDENCE REVIEW',
    brief: `Review the synthetic ${spec.lab} evidence and decide what is proven, what remains unknown, and which control corrects the trust failure.`,
    nodes: [
      { artifact:`BASELINE\n${spec.request}\n\nCOMPARISON\n${spec.compare}`, prompt:'Which evidence strategy produces the strongest conclusion?', choices:['Run unrelated payloads until something changes',spec.signal,'Assign severity from the vulnerability name'], answer:1, feedback:`The defensible path is ${spec.signal}. It changes one variable, retains provenance, and avoids expanding beyond the declared synthetic boundary.`, signal:'Controlled comparison selected' },
      { artifact:`OBSERVED SIGNAL\n${spec.focus}`, prompt:'How should the operator write the conclusion?', choices:[`The application is completely compromised`,`The designated behavior supports a specific ${spec.title.toLowerCase()} condition within the tested state`,`Every system using this technology is vulnerable`], answer:1, feedback:'The middle statement preserves scope and separates the observed condition from untested impact. A professional finding never converts a bounded proof into a universal claim.', signal:'Evidence-calibrated conclusion' },
      { artifact:`CONTROL REVIEW\n${spec.prevent}`, prompt:'Which remediation addresses the root decision?', choices:['Block the exact marker used in the lab',spec.control,'Hide the endpoint from the client interface'], answer:1, feedback:`The durable correction is to ${spec.control}. It repairs the trust boundary rather than the single demonstration string.`, signal:'Root control selected' },
    ],
  }
}

function makeLesson(spec, index) {
  return {
    id:`web-${spec.id}`, courseCode:'WEB-201', level:index<5?'FOUNDATION':index<18?'PRACTITIONER':'ADVANCED', title:spec.title, minutes:55, environment:`SEALED WEB RANGE // ${spec.lab.toUpperCase()}`,
    outcome:`Produce ${spec.artifact} and defend the conclusion under review.`,
    objectives:[`Explain why ${spec.focus}`,`Collect ${spec.signal}.`,`Design a corrective control that survives changes in payload and tooling.`],
    prerequisites:['Core scope and evidence lessons completed','Synthetic lab identity and fixtures only','No external target or production data'],
    sections:[
      { title:'Model the trust decision', body:`${spec.focus} The operator begins by naming the trusted component, the untrusted input, the interpreter or decision point, and the protected outcome. This model prevents a payload from replacing understanding.` },
      { title:'Hold the boundary constant', body:`${spec.boundary} Record the authorized identity, object, endpoint, method, state, and stop condition before validation. A result without these controls is difficult to reproduce and easy to overstate.` },
      { title:'Build discriminating evidence', body:`The required signal is ${spec.signal}. Establish a positive control first, change one meaningful variable, preserve the complete response, and include a negative control that distinguishes the suspected weakness from normal behavior.` },
      { title:'Repair and retest', body:`The primary control is to ${spec.control}. Retest the original comparison, verify intended behavior still works, and add a regression check at the layer that owns the security decision.` },
    ],
    steps:[
      { title:'Capture the positive control', instruction:'Establish the approved baseline before testing the boundary. Preserve status, relevant headers, body, identity, and timestamp.', command:spec.request, expected:'Baseline returns the designated synthetic behavior for the approved principal and object.', analysis:'A positive control proves the route, identity, and fixture work. Without it, a denial or empty response cannot distinguish security enforcement from a broken test.' },
      { title:'Change one security variable', instruction:'Run the designated comparison without changing identity, timing, transport, or unrelated headers.', command:spec.compare, expected:`Comparison exposes or rejects the ${spec.lab} boundary in a way that can be compared directly with baseline.`, analysis:`Interpret only the difference created by the controlled variable. The target evidence is ${spec.signal}; any broader impact remains a hypothesis until separately authorized.` },
      { title:'Write the smallest finding', instruction:'Record condition, prerequisite identity, affected decision, exact evidence, and what was not tested.', command:`dc-note --condition '${spec.id}' --evidence baseline.json,comparison.json`, expected:'A structured note links both artifacts and records the validation ceiling.', analysis:'A reviewable finding states the condition independently of the tool and keeps untested consequences out of the confirmed-impact field.' },
      { title:'Verify the durable control', instruction:'Translate the proposed repair into positive, negative, and regression tests at the authoritative layer.', command:`dc-retest --control '${spec.prevent.replaceAll("'",'')}'`, expected:'Positive behavior passes; the designated negative comparison is denied; unrelated workflow behavior remains stable.', analysis:`The repair standard is: ${spec.prevent} A payload-specific block is not sufficient because an equivalent representation may cross the same trust boundary.` },
    ],
    exercise:{ brief:`Complete the ${spec.lab} sealed scenario, then turn the raw transaction pair into ${spec.artifact}.`, deliverable:`${spec.artifact}, finding statement, remediation owner, and three-part retest plan.`, success:['One variable changes between baseline and comparison','Confirmed evidence is separated from untested impact','Remediation and retest target the authoritative decision layer'] },
    interactive:makeInteractive(spec),
    references:spec.refs.map((url,i)=>({label:i===0?'Primary standard or protocol':'Specialist lab reference',url})),
    check:{ q:`Which result is sufficient to complete the ${spec.title.toLowerCase()} lesson?`, options:['A scanner label without raw evidence',spec.artifact,'A list of unrelated payloads','A severity score copied from another environment'], answer:1, rationale:`The required artifact preserves the controlled evidence and the trust decision. It supports reproduction, review, remediation, and a meaningful retest without expanding scope.` },
    labId:spec.lab,
  }
}

export const webLessons = specs.map(makeLesson)

export const webCourse = {
  schemaVersion:1,
  code:'WEB-201',
  title:'Web & API Security Engineering',
  description:'Twenty-seven evidence-led modules spanning browser trust, server interpreters, identity, authorization, protocols, cache behavior, and resilient API design.',
  estimatedMinutes:webLessons.reduce((sum,lesson)=>sum+lesson.minutes,0),
  lessons:webLessons,
}
