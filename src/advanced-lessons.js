const step = (title, instruction, command, expected, analysis) => ({ title, instruction, command, expected, analysis })
const node = (prompt, artifact, choices, answer, feedback, signal) => ({ prompt, artifact, choices, answer, feedback, signal })
const scenario = (mode, title, brief, nodes) => ({ mode, title, brief, nodes })
const ref = (label, url) => ({ label, url })

export const advancedLessons = [
  {
    id: 'linux-privilege-paths', title: 'Linux privilege paths and proof', minutes: 60, level: 'ADVANCED', environment: 'SEALED LINUX PRIVILEGE RANGE',
    outcome: 'Build and validate a Linux privilege graph without changing protected state or relying on one-shot enumeration output.',
    objectives: ['Inventory identity and delegated authority', 'Connect writable resources to privileged execution', 'Distinguish capability, sudo, service, and scheduled-task paths', 'Write a minimal non-destructive proof'],
    prerequisites: ['Comfort with Linux permissions and processes', 'A disposable DaemonCore Linux target'],
    sections: [
      { title: 'Start from effective identity', body: 'Privilege analysis begins with the actual user, groups, namespaces, capabilities, and delegated commands. Record what the process can do now before searching for a path to something stronger.' },
      { title: 'Find controlling edges', body: 'A weak permission matters when it controls code, configuration, environment, or data that a stronger identity later consumes. Map writer, resource, consumer, and trigger as separate nodes.' },
      { title: 'Validate without taking over', body: 'Prefer configuration and execution-trace evidence over replacing binaries or spawning privileged shells. A reviewable proof identifies the controlling edge and protected consumer.' },
      { title: 'Remove the path, not the symptom', body: 'Remediation should eliminate untrusted control, reduce delegated commands, constrain capabilities, and protect deployment paths. Renaming files or hiding output does not change authority.' },
    ],
    steps: [
      step('Capture the starting authority', 'Record effective identity, groups, namespace context, capabilities, and explicit sudo delegation before interpreting any file permission.', `id && sudo -n -l 2>&1 && getpcaps $$`, `uid=1001(operator) groups=1001(operator),1100(release)\nUser operator may run: /usr/local/bin/release-check`, 'This establishes the exact starting node and one delegated execution edge. It does not yet prove escalation.'),
      step('Map privileged consumers', 'List synthetic services and timers with their execution identity and referenced paths, then keep only resources the starting identity can influence.', `systemctl list-timers --all --no-pager\nsystemctl cat dc-release.service`, `dc-release.timer → dc-release.service\nUser=root\nExecStart=/usr/local/bin/release-check /etc/dc/release.yml`, 'The service definition identifies a privileged consumer and two candidate control points: executable and configuration.'),
      step('Measure write authority', 'Inspect every component of the candidate path, including parent directories, without modifying it.', `namei -l /etc/dc/release.yml && getfacl -p /etc/dc/release.yml`, `group release has rw- on /etc/dc/release.yml\nservice consumes file as root`, 'Writable configuration plus privileged consumption forms a concrete path even if the timer is never triggered during validation.'),
      step('Package the proof', 'Write the path as subject, edge, consumer, consequence, and safe closure test.', `operator → group release → write /etc/dc/release.yml → dc-release.service(root)`, `One evidence-backed privilege path with no protected file changed.`, 'A path statement remains useful when tools change because it describes authority and control rather than a scanner label.'),
    ],
    exercise: { brief: 'Review a sealed host containing two harmless weak permissions and one reachable privileged control path.', deliverable: 'Produce a privilege graph, evidence index, minimal proof statement, and closure test.', success: ['Every edge cites command output', 'No privileged shell or protected-state change is required', 'The recommendation removes the controlling edge'] },
    references: [ref('Linux capabilities manual', 'https://man7.org/linux/man-pages/man7/capabilities.7.html'), ref('MITRE ATT&CK — Unix shell', 'https://attack.mitre.org/techniques/T1059/004/')],
    check: { q: 'Which evidence most directly proves a Linux privilege path?', options: ['A long enumeration report', 'Untrusted write control over input consumed by a stronger identity', 'An old package version', 'A root-owned filename'], answer: 1, rationale: 'The consequential condition is a reachable controlling edge into privileged execution.' },
    interactive: scenario('PRIVILEGE GRAPH', 'Linux control-path tribunal', 'Decide whether supplied host evidence forms a reachable privilege path and choose the smallest sufficient proof.', [
      node('Which pair creates the strongest candidate path?', 'operator is in release group; root timer consumes /etc/dc/release.yml; release group can write that file.', ['Writable log and root-owned service', 'Writable consumed configuration and root service', 'Old kernel and local account'], 1, 'Control of configuration consumed by a privileged service creates a specific reachable edge.', 'Privileged consumer edge isolated'),
      node('What is the safest validation?', 'The timer runs every five minutes in the sealed target.', ['Insert a shell command', 'Record ACL, service definition, and consumption relationship', 'Restart the host'], 1, 'Configuration evidence proves the authority path without executing a payload or changing protected state.', 'Non-destructive proof selected'),
      node('Which remediation closes the path?', 'The service must remain root and keep reading the configuration.', ['Restrict configuration writes to its deployment identity', 'Rename release.yml', 'Run the timer more often'], 0, 'Removing untrusted write authority breaks the controlling edge while preserving the required service behavior.', 'Control edge removed'),
    ]),
  },
  {
    id: 'windows-privilege-paths', title: 'Windows privilege paths and service control', minutes: 60, level: 'ADVANCED', environment: 'SEALED WINDOWS EVIDENCE WORKBENCH',
    outcome: 'Translate Windows tokens, service configuration, ACLs, and scheduled execution into defensible privilege paths.',
    objectives: ['Read effective token and integrity level', 'Analyze service and filesystem control independently', 'Trace scheduled execution and credential context', 'Recommend a control-specific repair'],
    prerequisites: ['Windows identity and ACL fundamentals', 'PowerShell familiarity'],
    sections: [
      { title: 'The token is the starting node', body: 'Users, groups, privileges, integrity level, and logon type define the current authority. Administrative group membership alone does not describe the effective process token.' },
      { title: 'Separate service controls', body: 'Service configuration permissions, binary permissions, parent-directory permissions, and service identity are different edges. Record each one before claiming a path.' },
      { title: 'Scheduled execution is a trust boundary', body: 'Tasks connect a trigger, principal, executable, arguments, working directory, and credentials. Weakness appears when an untrusted principal controls an input used by a stronger task identity.' },
      { title: 'Prove with descriptors', body: 'Security descriptors and configuration output can establish a path without replacing executables or starting services. Preserve SDDL or normalized ACL evidence with the system role.' },
    ],
    steps: [
      step('Record effective token', 'Capture user, groups, privileges, integrity level, and logon context from the designated synthetic workstation.', `$env:COMPUTERNAME; whoami /all`, `User: DC-LAB\\operator\nIntegrity: Medium\nGroups: Operators\nPrivileges: SeChangeNotifyPrivilege`, 'The token output establishes current authority and prevents an assumed administrator role from contaminating the analysis.'),
      step('Join service identity to configuration', 'Inventory only the designated service and preserve its account, start mode, executable path, and arguments.', `Get-CimInstance Win32_Service -Filter "Name='DCRelease'" | Select Name,StartName,StartMode,PathName`, `DCRelease | LocalSystem | Auto | C:\\ProgramData\\DC\\release-agent.exe --config release.yml`, 'A LocalSystem consumer is significant only after control over its executable or configuration is measured.'),
      step('Measure every ACL edge', 'Inspect the service executable, configuration, and parent directories as separate resources.', `Get-Acl 'C:\\ProgramData\\DC\\release.yml' | Format-List Owner,AccessToString`, `BUILTIN\\Operators Allow Modify\nNT AUTHORITY\\SYSTEM FullControl`, 'Operators can influence a file consumed by the LocalSystem service. The descriptor is stronger evidence than attempting replacement.'),
      step('Write the closure test', 'Define both the denied untrusted action and the preserved deployment workflow after remediation.', `Operators: write denied\nDCDeploy: atomic update allowed\nDCRelease: starts successfully`, `A negative and positive control that tests the intended boundary.`, 'A useful repair test proves the weak edge is gone without breaking the authorized software-delivery path.'),
    ],
    exercise: { brief: 'Analyze a synthetic Windows service, scheduled task, and three ACL records to identify the one complete privilege path.', deliverable: 'Submit a token baseline, path graph, SDDL evidence index, and two-sided closure test.', success: ['Token state is recorded before path analysis', 'Service and file controls are not conflated', 'No executable replacement is performed'] },
    references: [ref('Microsoft service security and access rights', 'https://learn.microsoft.com/en-us/windows/win32/services/service-security-and-access-rights'), ref('Microsoft access control model', 'https://learn.microsoft.com/en-us/windows/win32/secauthz/access-control-model')],
    check: { q: 'What completes a Windows service privilege path?', options: ['A service runs automatically', 'A lower-trust principal controls content or configuration consumed by a stronger service identity', 'The binary is unsigned', 'The service name is writable text'], answer: 1, rationale: 'The path requires both lower-trust control and verified consumption by a stronger service identity.' },
    interactive: scenario('SERVICE GRAPH', 'Windows authority mapper', 'Evaluate token, service, task, and ACL evidence without modifying the sealed workstation.', [
      node('Which evidence is still missing?', 'DCRelease runs as LocalSystem. The binary path is C:\\ProgramData\\DC\\agent.exe.', ['The binary and parent-directory ACLs', 'The desktop wallpaper', 'The OS product key'], 0, 'Service identity and path do not prove control. ACL evidence is required to connect the starting principal.', 'Missing ACL edge identified'),
      node('Operators have Modify on release.yml consumed at startup. What is sufficient proof?', 'Restart permission is held only by administrators.', ['Replace the file and wait', 'Preserve ACL and service configuration showing privileged consumption', 'Guess an administrator password'], 1, 'The descriptor and consumption relationship prove the condition without requiring execution.', 'Configuration proof accepted'),
      node('Which primary fix closes this control path?', 'Authorized deployment still needs to update release.yml.', ['Grant Everyone FullControl', 'Assign writes only to a dedicated deployment identity', 'Move the shortcut'], 1, 'A dedicated deployment identity preserves function while removing the lower-trust controlling edge.', 'Deployment boundary restored'),
    ]),
  },
  {
    id: 'active-directory-graphs', title: 'Active Directory control graphs', minutes: 60, level: 'ADVANCED', environment: 'SYNTHETIC DIRECTORY GRAPH RANGE',
    outcome: 'Convert directory membership, delegation, ACL, and certificate relationships into prioritized control paths.',
    objectives: ['Normalize principals and security identifiers', 'Trace nested membership and delegated rights', 'Separate reachable paths from theoretical edges', 'Recommend path reduction and administrative tiering'],
    prerequisites: ['Windows domain fundamentals', 'Authorization to use synthetic directory exports'],
    sections: [
      { title: 'Directory privilege is transitive', body: 'A principal may control a protected identity through nested groups, delegated object rights, service ownership, policy, or certificate enrollment. Evaluate the chain, not the display name.' },
      { title: 'Identity resolution comes first', body: 'Resolve SIDs, aliases, stale objects, and group scope before building paths. Incorrect identity merging creates convincing but false control relationships.' },
      { title: 'Reachability changes priority', body: 'A graph edge matters when the starting principal can exercise it under current conditions. Mark prerequisites, session location, policy restrictions, and monitoring rather than assuming them away.' },
      { title: 'Reduce durable control', body: 'Prefer removing unnecessary delegation, isolating administrative identities, constraining enrollment, and shortening nested paths. Detection supports but does not replace path reduction.' },
    ],
    steps: [
      step('Normalize the starting principal', 'Resolve the designated synthetic account to SID, direct groups, and nested effective groups.', `Get-ADUser dc-auditor -Properties tokenGroups | Select SamAccountName,SID,tokenGroups`, `dc-auditor | S-1-5-21-...-1142 | Audit-Operators → App-Release`, 'Stable identifiers prevent renamed or duplicated display names from creating false graph nodes.'),
      step('Inspect delegated object control', 'Export only ACEs involving the starting groups and the designated application OU.', `Get-Acl 'AD:\\OU=Applications,DC=dc,DC=lab' | Select -Expand Access`, `App-Release Allow WriteProperty servicePrincipalName on svc-checkout`, 'The ACE supplies a specific action, object, and controlling principal instead of a generic high-risk label.'),
      step('Join prerequisites into the graph', 'Record whether the edge requires a session, credential, host location, or enrollment policy before ranking it.', `dc-auditor → App-Release → WriteProperty(svc-checkout)\nPrerequisite: interactive logon to PAW-02\nObserved session: none`, `Reachable authority is separated from an unmet execution prerequisite.`, 'The graph remains useful without overstating immediate exploitability.'),
      step('Prioritize path removal', 'Choose the smallest directory change that removes the control edge and define a permission regression test.', `Remove App-Release WriteProperty SPN on svc-checkout\nAdd deployment workflow with approval\nRetest denied write + successful release`, `A reduced graph and preserved business workflow.`, 'Deleting the unnecessary delegated ACE is stronger than merely alerting when it is used.'),
    ],
    exercise: { brief: 'Analyze a synthetic directory export containing nested groups, stale SIDs, delegated rights, and certificate enrollment edges.', deliverable: 'Produce a normalized graph with one prioritized path, prerequisites, and a path-removal plan.', success: ['Every node uses a stable identifier', 'Unmet prerequisites remain explicit', 'Remediation removes at least one durable control edge'] },
    references: [ref('Microsoft privileged access strategy', 'https://learn.microsoft.com/en-us/security/privileged-access-workstations/privileged-access-access-model'), ref('Microsoft Active Directory security assessment', 'https://learn.microsoft.com/en-us/services-hub/microsoft-engage-center/health/getting-started-adsecurity')],
    check: { q: 'What makes an Active Directory graph path defensible?', options: ['A red edge in a visualization', 'Resolved principals, explicit rights, protected objects, and stated prerequisites', 'A group containing many users', 'An administrator display name'], answer: 1, rationale: 'A path must preserve identity, authority, target, and reachability evidence.' },
    interactive: scenario('DIRECTORY GRAPH', 'Delegation path analyzer', 'Resolve synthetic directory relationships and prioritize only paths supported by effective authority.', [
      node('Which identifier should anchor the graph node?', 'Display name App Admins exists twice in separate domains.', ['Display name', 'Security identifier plus domain context', 'Email alias'], 1, 'The SID and domain context distinguish principals even when names collide.', 'Directory identity normalized'),
      node('Which edge is actionable?', 'Audit-Operators can write SPN on svc-checkout; use requires PAW-02; no operator session exists there.', ['Immediately exploitable', 'Valid control edge with unmet execution prerequisite', 'Not an edge'], 1, 'The authority is real, while present reachability remains constrained by the missing prerequisite.', 'Reachability calibrated'),
      node('Which recommendation most reduces risk?', 'Monitoring already alerts on SPN changes.', ['Keep delegation because it is monitored', 'Remove unnecessary delegation and preserve an approved workflow', 'Rename the service account'], 1, 'Prevention removes durable control; monitoring remains a supporting measure.', 'Directory path shortened'),
    ]),
  },
  {
    id: 'kerberos-trust-decisions', title: 'Kerberos tickets and trust decisions', minutes: 60, level: 'ADVANCED', environment: 'SYNTHETIC KERBEROS EVIDENCE LAB',
    outcome: 'Read Kerberos ticket, service identity, delegation, and time evidence without treating ticket possession as universal authority.',
    objectives: ['Trace client, KDC, service, and ticket roles', 'Interpret SPNs and ticket scope', 'Evaluate delegation prerequisites', 'Separate authentication proof from service authorization'],
    prerequisites: ['Active Directory identity fundamentals', 'A synthetic ticket and directory evidence pack'],
    sections: [
      { title: 'Tickets bind specific claims', body: 'A ticket identifies a client, service, realm, times, flags, and cryptographic context. It does not automatically authorize every action at the destination service.' },
      { title: 'SPNs name service identities', body: 'Duplicate, stale, or incorrectly assigned SPNs can redirect authentication assumptions. Resolve the account and service placement before drawing conclusions.' },
      { title: 'Delegation has conditions', body: 'Constrained, resource-based, and unconstrained delegation create different trust relationships. Document allowed services, controlling principals, protocol transition, and host protection.' },
      { title: 'Time and revocation matter', body: 'Ticket validity, clock skew, account changes, and service-side authorization affect observed behavior. Preserve timestamps and distinguish cached evidence from current policy.' },
    ],
    steps: [
      step('Read the ticket cache', 'Capture the synthetic client, server principal, realm, validity, and flags from the designated session.', `whoami /user; klist`, `Client: dc-auditor@DC.LAB\nServer: HTTP/portal.dc.lab@DC.LAB\nValid: 14:00Z–22:00Z\nFlags: forwardable renewable`, 'The ticket is scoped to one service principal and time interval; it is not a general proof of portal permissions.'),
      step('Resolve service identity', 'Map the SPN to its directory account and designated host before analyzing delegation.', `Get-ADObject -LDAPFilter '(servicePrincipalName=HTTP/portal.dc.lab)' -Properties servicePrincipalName`, `CN=svc-portal | HTTP/portal.dc.lab | host=portal-01`, 'One resolved service account provides a stable node for the trust graph.'),
      step('Inspect delegation boundaries', 'Record the controlling principal and exact allowed service rather than labeling the host broadly delegatable.', `Get-ADComputer portal-01 -Properties PrincipalsAllowedToDelegateToAccount,msDS-AllowedToDelegateTo`, `RBCD controller: app-deploy\nAllowed service: MSSQLSvc/reporting.dc.lab`, 'Delegation evidence is meaningful only with the source principal, destination service, and required ticket conditions.'),
      step('Separate service authorization', 'Compare successful service authentication with the application role decision from synthetic logs.', `authn=success principal=dc-auditor\nauthz=deny action=export role=reader`, `Kerberos identity accepted; export authorization denied.`, 'Authentication confirms the service accepted an identity claim. The application still owns the action decision.'),
    ],
    exercise: { brief: 'Reconstruct a synthetic Kerberos exchange containing an SPN conflict, a delegation edge, and an application denial.', deliverable: 'Submit a ticket timeline, service-identity map, delegation prerequisites, and final authorization conclusion.', success: ['Ticket scope and validity are recorded', 'SPN identity is resolved before path analysis', 'Authentication is not confused with application authorization'] },
    references: [ref('Microsoft Kerberos authentication overview', 'https://learn.microsoft.com/en-us/windows-server/security/kerberos/kerberos-authentication-overview'), ref('Microsoft Kerberos constrained delegation overview', 'https://learn.microsoft.com/en-us/windows-server/security/kerberos/kerberos-constrained-delegation-overview')],
    check: { q: 'What does a valid Kerberos service ticket prove most directly?', options: ['The client may perform every service action', 'The KDC issued a ticket for the named client and service under stated conditions', 'The service account is secure', 'Delegation is enabled'], answer: 1, rationale: 'Application authorization remains a separate service-side decision.' },
    interactive: scenario('TICKET TRACE', 'Kerberos trust interpreter', 'Resolve service tickets, SPNs, delegation edges, and application decisions from synthetic evidence.', [
      node('What is the strongest ticket conclusion?', 'Server: HTTP/portal.dc.lab; end time 22:00Z; forwardable flag set.', ['Universal domain access', 'A time-bounded ticket for the named service that may be forwarded under policy', 'Portal administrator role'], 1, 'The ticket describes a scoped authentication claim and flags, not application privileges.', 'Ticket scope calibrated'),
      node('Two accounts own the same SPN. What should happen first?', 'svc-portal and old-portal both list HTTP/portal.dc.lab.', ['Choose the newer account', 'Treat the conflict as identity ambiguity and resolve ownership', 'Request tickets repeatedly'], 1, 'Duplicate SPNs undermine deterministic service identity and must be resolved before trust analysis.', 'SPN conflict preserved'),
      node('Authentication succeeds and export is denied. Which control acted?', 'KDC issued ticket; portal log says role reader cannot export.', ['Kerberos failed', 'Application authorization denied the action', 'DNS blocked access'], 1, 'The service accepted the identity and independently denied the requested operation.', 'Authentication and authorization separated'),
    ]),
  },
  {
    id: 'segmentation-pivot-analysis', title: 'Segmentation and pivot-path analysis', minutes: 60, level: 'ADVANCED', environment: 'SEALED MULTI-ZONE NETWORK MODEL',
    outcome: 'Prove or reject a network pivot path using routes, listeners, policy intent, and bounded reachability evidence.',
    objectives: ['Establish vantage and routing context', 'Differentiate reachability from authorization', 'Map multi-hop prerequisites', 'Write a segmentation closure test'],
    prerequisites: ['TCP/IP and routing fundamentals', 'An isolated multi-zone range'],
    sections: [
      { title: 'Vantage defines the observation', body: 'Routes, interfaces, DNS views, proxies, and policy differ between hosts. Every reachability statement must preserve the system and identity that produced it.' },
      { title: 'A pivot is a dependency chain', body: 'A complete path joins initial access, an available forwarding mechanism, an allowed route, a reachable service, and usable service authorization. Missing links must stay visible.' },
      { title: 'Test the narrow edge', body: 'Use designated synthetic services and exact ports to validate segmentation. Broad discovery can hide which policy decision actually failed.' },
      { title: 'Retest both directions', body: 'After repair, prove the prohibited path is denied and the approved operational flow still works. A single timeout is not sufficient evidence of correct policy.' },
    ],
    steps: [
      step('Capture vantage and routes', 'Record interfaces, routes, resolver, and proxy state on the designated operator node before testing another zone.', `ip -brief address && ip route && env | grep -i proxy`, `operator: 10.60.1.20/24\ndefault via 10.60.1.1\n10.60.2.0/24 via 10.60.1.10`, 'The route identifies a candidate forwarding node. It does not establish whether policy or service authorization permits the path.'),
      step('Confirm the forwarding prerequisite', 'Inspect the designated relay for listening and forwarding state without creating a new tunnel.', `ss -lntp && sysctl net.ipv4.ip_forward`, `10.60.1.10:8443 LISTEN\nnet.ipv4.ip_forward = 1`, 'A forwarding-capable node exists, satisfying one path prerequisite while leaving destination policy unresolved.'),
      step('Test one policy edge', 'Send a single bounded request to the designated synthetic health service and preserve source, destination, port, and response.', `curl --connect-timeout 2 -sS http://10.60.2.40:8080/health`, `HTTP 200\nzone=payments-lab\nsource=operator-via-relay`, 'The response proves the exact path is reachable from this vantage. It does not imply broader subnet access.'),
      step('Define the two-sided retest', 'Pair a prohibited cross-zone denial with an approved monitoring flow that must remain functional.', `operator→payments:8080 DENY\nmonitor→payments:8080 ALLOW`, `One negative and one positive control measured from named vantages.`, 'Two-sided testing distinguishes a correct segmentation repair from a service outage.'),
    ],
    exercise: { brief: 'Evaluate a synthetic three-zone topology containing one intended monitoring route and one unintended operator route.', deliverable: 'Produce a hop-by-hop path, missing or satisfied prerequisites, and two-sided segmentation retest.', success: ['Every observation names its vantage', 'Only designated ports are tested', 'Approved operational connectivity is included in closure testing'] },
    references: [ref('NIST SP 800-115 — Technical security testing', 'https://csrc.nist.gov/pubs/sp/800/115/final'), ref('CISA Zero Trust Maturity Model', 'https://www.cisa.gov/resources-tools/resources/zero-trust-maturity-model')],
    check: { q: 'What is required to claim a usable network pivot path?', options: ['One reachable IP', 'A complete chain of access, forwarding, routing, service reachability, and required authorization', 'A traceroute screenshot', 'Two interfaces on a host'], answer: 1, rationale: 'Each prerequisite must be supported for the path to be operationally credible.' },
    interactive: scenario('PATH PROVER', 'Segmentation path tribunal', 'Join routing, relay, service, and policy evidence without expanding beyond designated synthetic edges.', [
      node('What is missing from this path?', 'Operator routes payments zone via relay. Relay forwards packets. Destination evidence is absent.', ['A destination reachability result', 'A larger subnet scan', 'A public exploit'], 0, 'Routing and forwarding satisfy intermediate prerequisites, but the destination edge remains unmeasured.', 'Missing destination edge isolated'),
      node('Which test is most precise?', 'Authorized target payments-health:8080; no other ports are in scope.', ['Scan the zone', 'One request to payments-health:8080 from the named vantage', 'Enumerate relay credentials'], 1, 'The narrow request measures the exact disputed policy edge with minimal noise.', 'Bounded edge test selected'),
      node('Which result set actually proves the segmentation fix?', 'Firewall rule changed after the finding.', ['Operator path denied and monitoring path still allowed', 'Operator path times out once', 'Rule count increased'], 0, 'A negative and positive control prove both isolation and required function.', 'Two-sided closure proven'),
    ]),
  },
  {
    id: 'ssrf-internal-trust', title: 'SSRF and internal trust boundaries', minutes: 60, level: 'ADVANCED', environment: 'SEALED WEB-TO-METADATA RANGE',
    outcome: 'Trace server-side request behavior through parsing, resolution, redirects, and destination trust using harmless synthetic endpoints.',
    objectives: ['Locate the server-side fetch primitive', 'Model URL parsing and resolution stages', 'Validate only designated internal destinations', 'Recommend destination-based controls'],
    prerequisites: ['HTTP request and URL parsing fundamentals', 'A sealed synthetic web target'],
    sections: [
      { title: 'The server becomes the vantage', body: 'SSRF risk appears when attacker-influenced input causes a trusted server to initiate a request. The destination sees the server network, identity, DNS, proxy, and credentials.' },
      { title: 'Validation must survive transformations', body: 'Scheme parsing, decoding, DNS resolution, redirects, proxies, and connection reuse can change the effective destination. Controls must evaluate the final connection, not only the submitted string.' },
      { title: 'Internal responses are not equal', body: 'A harmless health endpoint, metadata service, administrative API, and callback sink carry different consequences. Use synthetic targets and stop at the first sufficient trust-boundary proof.' },
      { title: 'Build a safe fetcher', body: 'Prefer explicit destination allowlists, resolution and IP classification at connection time, redirect restrictions, credential stripping, egress controls, and response limits.' },
    ],
    steps: [
      step('Map the fetch contract', 'Inspect the designated endpoint and record accepted schemes, redirect behavior, response limits, and server-side identity.', `curl -s http://fetch-target:8080/schema`, `url: string\nschemes: http,https\nredirects: 3\nresponse_limit: 1MB\nserver_identity: fetch-worker`, 'The contract identifies the attacker-controlled value and the transformations that require validation.'),
      step('Establish the public control', 'Request the Academy-provided public-style synthetic endpoint to prove normal fetch behavior.', `curl -s 'http://fetch-target:8080/fetch?url=http://public-sink:8081/health'`, `fetched=true\ndestination=public-sink:8081\nclassification=synthetic-public`, 'The positive control proves the feature works before a boundary test is interpreted.'),
      step('Test one internal boundary', 'Request only the designated synthetic metadata endpoint and stop after operational metadata is returned.', `curl -s 'http://fetch-target:8080/fetch?url=http://metadata-target:8082/identity'`, `role=dc-training-worker\nclassification=synthetic-metadata`, 'The response proves the fetch worker can cross the intended internal boundary. No secrets or write operations are needed.'),
      step('Design final-destination enforcement', 'Write the sequence the service must apply for direct requests and redirects.', `parse → scheme allowlist → resolve → classify every address → connect pinned address → reject redirects across class`, `A control sequence tied to the effective destination.`, 'String filters are insufficient when resolution or redirects can change where the server connects.'),
    ],
    exercise: { brief: 'Review five synthetic URL cases involving encoding, mixed DNS answers, redirects, and userinfo ambiguity.', deliverable: 'Produce a transformation table, one safe proof, and final-destination validation algorithm.', success: ['The public positive control is preserved', 'No real metadata or external system is contacted', 'Redirect and DNS revalidation are explicit'] },
    references: [ref('OWASP SSRF Prevention Cheat Sheet', 'https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html'), ref('OWASP WSTG — Testing for SSRF', 'https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/07-Input_Validation_Testing/19-Testing_for_Server-Side_Request_Forgery')],
    check: { q: 'Where must an SSRF defense classify the destination?', options: ['Only in the original URL text', 'At every resolution and redirect step used for the effective connection', 'Only after the response arrives', 'In the browser'], answer: 1, rationale: 'Transformations can change the effective destination after initial parsing.' },
    interactive: scenario('FETCH TRACE', 'Server-side destination analyzer', 'Follow synthetic fetch inputs through parsing, DNS, redirects, and connection policy.', [
      node('Which identity performs the risky connection?', 'Browser submits url parameter; fetch-worker resolves and connects.', ['Browser user', 'fetch-worker service identity and network vantage', 'DNS administrator'], 1, 'The server-side worker originates the destination request and carries its trust context.', 'Server vantage identified'),
      node('A public hostname resolves to public and private addresses. What should happen?', 'Resolver returns 198.51.100.40 and 10.60.2.10.', ['Choose public address', 'Reject the mixed classification before connecting', 'Try both'], 1, 'Mixed answers can cross a trust boundary and should fail closed under a public-only policy.', 'Mixed resolution blocked'),
      node('A permitted URL redirects to metadata-target. Which policy is correct?', 'Initial destination is synthetic-public; redirect target is synthetic-internal.', ['Follow because the first URL passed', 'Revalidate and reject the class-changing redirect', 'Strip the path only'], 1, 'Every redirect introduces a new effective destination requiring the same policy checks.', 'Redirect boundary enforced'),
    ]),
  },
  {
    id: 'oauth-oidc-flows', title: 'OAuth and OIDC flow validation', minutes: 60, level: 'ADVANCED', environment: 'SYNTHETIC IDENTITY PROVIDER LAB',
    outcome: 'Validate OAuth and OIDC clients by tracing actors, redirects, state, nonce, token audience, and session binding.',
    objectives: ['Name every actor and artifact in the flow', 'Validate redirect and browser-state protections', 'Interpret token issuer and audience', 'Test logout and revocation behavior'],
    prerequisites: ['HTTP and session fundamentals', 'Basic JSON Web Token structure'],
    sections: [
      { title: 'Name the protocol actors', body: 'Resource owner, browser, client, authorization server, and resource server make different decisions. Confusing their roles leads to invalid test conclusions.' },
      { title: 'Bind browser state', body: 'State, nonce, PKCE, exact redirect registration, and transaction storage prevent different substitution and replay problems. Record which component creates and validates each value.' },
      { title: 'Validate token context', body: 'Signature validity is only one requirement. Issuer, audience, authorized party, time, nonce, and token type must match the receiving component and transaction.' },
      { title: 'Close the lifecycle', body: 'Logout, account suspension, consent removal, key rotation, and refresh-token revocation should produce defined session behavior. Test designated synthetic identities and bounded transitions.' },
    ],
    steps: [
      step('Draw the actor sequence', 'Record who creates, carries, and validates authorization request, code, and tokens for the synthetic client.', `browser → client /login\nclient → issuer /authorize + state + PKCE\nissuer → client /callback + code\nclient → issuer /token\nclient → resource API`, `Five actors and artifacts with no implied trust.`, 'A sequence diagram makes it clear which component owns each validation.'),
      step('Inspect redirect binding', 'Compare the exact registered redirect with the authorization request and preserve state and PKCE metadata.', `registered: https://client.dc.lab/callback\nrequested: https://client.dc.lab/callback\nstate: 128-bit random\nPKCE: S256`, `Exact redirect match and transaction-bound browser state.`, 'Prefix or wildcard redirect matching can transfer authorization artifacts to unintended destinations.'),
      step('Decode claims without treating them as trusted', 'Decode the synthetic ID token payload, then evaluate claims against the client expectation.', `printf '%s' "$ID_TOKEN_PAYLOAD" | base64 -d | jq '{iss,aud,azp,exp,nonce}'`, `iss=https://issuer.dc.lab\naud=dc-client\nazp=dc-client\nnonce=txn-042`, 'Decoded claims are evidence inputs. Signature and issuer metadata validation must occur before trust.'),
      step('Test lifecycle closure', 'Use the designated account to revoke consent and verify refresh, client session, and resource access outcomes.', `consent=revoke → refresh=denied\nclient_session=ended\nresource_token=expires_at_original_deadline`, `A documented transition for every surviving artifact.`, 'Revocation semantics differ by artifact. The report should state exactly what stopped immediately and what remained time-bounded.'),
    ],
    exercise: { brief: 'Analyze a synthetic authorization-code flow containing one redirect mismatch, one nonce error, and one valid resource denial.', deliverable: 'Submit an actor diagram, validation matrix, failing control, and lifecycle retest.', success: ['Every artifact has a creator and validator', 'Decoded token content is not assumed trusted', 'Revocation expectations are artifact-specific'] },
    references: [ref('OAuth 2.0 Security Best Current Practice', 'https://www.rfc-editor.org/rfc/rfc9700'), ref('OpenID Connect Core 1.0', 'https://openid.net/specs/openid-connect-core-1_0.html')],
    check: { q: 'Which checks make an ID token meaningful to a client?', options: ['Base64 decoding alone', 'Signature plus expected issuer, audience, time, nonce, and transaction context', 'A recognizable email claim', 'HTTPS on the landing page'], answer: 1, rationale: 'A valid token must be cryptographically and contextually bound to the receiver and transaction.' },
    interactive: scenario('FLOW VALIDATOR', 'OAuth transaction tribunal', 'Trace a synthetic authorization-code flow and decide which component owns each validation.', [
      node('Who validates state on callback?', 'Client generated state before redirect; issuer returns it unchanged.', ['Resource server', 'Client transaction handler', 'Browser extension'], 1, 'The client created state to bind the browser transaction and must compare it on callback.', 'State validator assigned'),
      node('Token signature is valid but aud names another client. What is the disposition?', 'aud=reporting-client; receiver=checkout-client.', ['Accept because issuer is trusted', 'Reject the token for audience mismatch', 'Rewrite the claim'], 1, 'A valid signature does not authorize a token for the wrong receiver.', 'Audience mismatch rejected'),
      node('Consent is revoked but an access token remains valid for three minutes. What should the report say?', 'Refresh is denied immediately; resource server does not introspect.', ['Revocation failed completely', 'Refresh stopped immediately; existing token remains valid until its bounded expiry', 'Token is invalid because the UI says logged out'], 1, 'The artifact-specific behavior is precise and supports an appropriate remediation decision.', 'Lifecycle behavior calibrated'),
    ]),
  },
  {
    id: 'cicd-provenance', title: 'CI/CD identity and provenance chains', minutes: 60, level: 'ADVANCED', environment: 'SEALED SOFTWARE FACTORY WORKBENCH',
    outcome: 'Trace source, workflow, runner, artifact, registry, signature, and deployment identities into one verifiable release chain.',
    objectives: ['Separate source and build identities', 'Evaluate runner and secret boundaries', 'Bind attestations to exact artifact digests', 'Design promotion and rollback verification'],
    prerequisites: ['Container and supply-chain fundamentals', 'JSON and digest inspection skills'],
    sections: [
      { title: 'Every stage has an identity', body: 'Commit author, repository workflow, automation token, runner, builder, registry, signer, and deployer are distinct principals. Trust must not collapse into one generic pipeline identity.' },
      { title: 'Runners are execution boundaries', body: 'Persistent workspaces, privileged containers, broad tokens, untrusted pull requests, and shared caches can join attacker-controlled input to protected credentials or artifacts.' },
      { title: 'Provenance binds subjects', body: 'An attestation is useful only when its trusted signer, builder identity, source material, parameters, and subject digest match the artifact being promoted.' },
      { title: 'Promotion is a policy decision', body: 'Deployment should verify immutable subject identity and required evidence at every environment boundary. Tags and filenames are references, not stable artifact identity.' },
    ],
    steps: [
      step('Map factory principals', 'List the identity and trust boundary for source approval, workflow execution, build, signing, registry write, and deployment.', `source-reviewer → workflow-ref\nOIDC runner → builder\nbuilder → artifact digest\nsigner → attestation\ndeployer → environment`, `A chain of distinct principals and signed subjects.`, 'Separating identities reveals where one broad credential bypasses multiple intended approvals.'),
      step('Inspect runner authority', 'Record token audience, repository scope, workflow ref, environment, and workspace lifetime from the synthetic job evidence.', `jq '{repository,workflow_ref,environment,token_aud,workspace_reuse}' job.json`, `repository=checkout\nworkflow_ref=refs/heads/main\ntoken_aud=artifact-registry\nworkspace_reuse=false`, 'Short-lived, audience-bound identity and clean workspaces reduce cross-job trust leakage.'),
      step('Bind provenance to the subject', 'Compare release digest, SBOM subject, and trusted attestation subject exactly.', `jq -r '.digest' release.json\njq -r '.subject' sbom.json\njq -r '.trusted[].subject' attestations.json`, `release=sha256:dc042\nsbom=sha256:dc042\ntrusted_attestations=[sha256:dc041,sha256:dc040]`, 'Integrity of individual files does not close the provenance gap when trusted evidence names different subjects.'),
      step('Write the promotion gate', 'Define required evidence and immutable comparisons before deployment proceeds.', `subject digest exact match\ntrusted builder identity\napproved workflow ref\nSBOM subject match\npolicy result allow`, `A fail-closed gate evaluated again at promotion time.`, 'Verification at deployment prevents an earlier checked tag from being rebound to a different artifact.'),
    ],
    exercise: { brief: 'Review a synthetic release bundle with valid checksums, a matching SBOM, stale trusted attestations, and a mutable deployment tag.', deliverable: 'Produce a principal map, subject comparison table, failed promotion decision, and corrected gate.', success: ['Tags are not treated as immutable identity', 'Every attestation is matched to its subject', 'The deployment gate fails closed on missing evidence'] },
    references: [ref('SLSA specification', 'https://slsa.dev/spec/'), ref('Sigstore Cosign verification', 'https://docs.sigstore.dev/cosign/verifying/verify/')],
    check: { q: 'What must a provenance verifier match before promoting an artifact?', options: ['Repository name only', 'Trusted signer and builder claims to the exact artifact subject digest and policy', 'A familiar image tag', 'The latest SBOM filename'], answer: 1, rationale: 'Provenance is bound to immutable subjects and trusted identities, not mutable references.' },
    interactive: scenario('PROMOTION GATE', 'Software factory verifier', 'Evaluate synthetic runner identity, artifact subjects, attestations, and deployment policy.', [
      node('Which value is the stable release identity?', 'tag=checkout:latest; digest=sha256:dc042.', ['checkout:latest', 'sha256:dc042', 'release.zip'], 1, 'The digest identifies immutable artifact content; tags can be rebound.', 'Immutable subject selected'),
      node('SBOM matches dc042, trusted attestation names dc041. What is the promotion result?', 'All evidence files have valid SHA-256 checksums.', ['Allow because files are intact', 'Deny because trusted provenance does not cover the promoted subject', 'Rewrite attestation subject'], 1, 'File integrity does not make evidence applicable to a different artifact digest.', 'Subject mismatch denied'),
      node('Which runner control most limits credential exposure?', 'Pull requests can modify build steps.', ['Persistent administrator token', 'Short-lived audience-bound identity unavailable to untrusted workflows', 'Shared writable workspace'], 1, 'Ephemeral, scoped identity and workflow trust checks prevent attacker-controlled jobs from inheriting protected authority.', 'Runner identity boundary enforced'),
    ]),
  },
]
