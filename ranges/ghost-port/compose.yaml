name: daemoncore-ghost-port

services:
  operator:
    build:
      context: ./operator
    container_name: dc-ghost-operator
    command: ["sleep", "infinity"]
    networks: [range]
    read_only: true
    tmpfs:
      - /tmp:size=64m,noexec,nosuid,nodev
      - /workspace:size=32m,nosuid,nodev
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges:true
    pids_limit: 128
    mem_limit: 384m
    cpus: 0.75
    restart: "no"

  archive-target:
    build:
      context: ./target
    container_name: dc-ghost-target
    networks:
      range:
        aliases: [archive-target]
    read_only: true
    tmpfs:
      - /tmp:size=32m,noexec,nosuid,nodev
    cap_drop: [ALL]
    cap_add: [NET_BIND_SERVICE]
    security_opt:
      - no-new-privileges:true
    pids_limit: 64
    mem_limit: 192m
    cpus: 0.50
    restart: "no"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8088/health"]
      interval: 2s
      timeout: 1s
      retries: 20
      start_period: 2s

networks:
  range:
    name: dc-ghost-range
    driver: bridge
    internal: true
